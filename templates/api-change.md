# API 变更记录：<编号>

状态：DRAFT / APPROVED / IMPLEMENTED / VERIFIED  
关联任务/需求：  
变更前契约版本：  
拟变更版本：

影响 method/path：  
新增/删除/改名字段与类型：  
默认值、可空性和枚举：  
HTTP 错误与业务错误：  
expected_revision、Idempotency-Key、重复提交语义：  
SSE 事件与断线恢复：  
输入权限、PII 和模型外发：  
迁移和旧数据策略：  
前端行为变化：  
破坏性变更和回滚：

同步清单：api.md → Pydantic DTO → 导出的 OpenAPI → TS 类型 → API 调用处 → 反例测试 → process.md → CHANGELOG.md。

真实测试记录：  
独立验收结论：
