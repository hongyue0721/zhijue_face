# 给新会话 AI 的续接指令

请从 AGENTS.md 和 process.md 恢复工程现状，并检查实际 Git 状态、当前代码和最近验收证据。不要仅相信前一个 AI 写的“已完成”。

本次只领取 process.md 中第一个依赖确实通过的未完成任务。使用 templates/task-card.md 说明范围、契约影响和测试方法，再实施。明确区分文档规范、代码已存在和实际测试已通过。

任何 API 字段/状态/错误/事件更改同步 api.md、Pydantic、生成类型、契约测试；任何功能完成同步 process.md 和 CHANGELOG.md。没变更的接口登记“已检查，无变化”，不伪造文档更新。

不得擅自扩岗位、题库规模、OCR、长期记忆或联网检索；不得为了完成目标改成 mock、关闭 TLS、丢掉引文校验或吞掉异常。

收工使用 templates/handoff.md，附实际结果与未运行项目。下一任务只推荐一个，避免把未来计划混入完成清单。
