# AI 施工交接：2026-09-19 M2-01 Level 2 来源审核准备

## 当前状态

工作目录/分支：仓库根 / `master`
基线 commit：`7ac1b7f`  
本次 commit 或尚未提交的文件：尚未提交；保留此前 M2 连续工作区差异，未 reset、clean、stash 或覆盖用户内容  
应用版本 / 规范版本：`0.1.0` / `1.0.0`  
当前阶段和任务状态：M2 / IN_PROGRESS；M2-01 Level 2 材料 `READY_FOR_OWNER_REVIEW`，正式 Level 2 仍 `pending`；未自行写 `approved`  
运行模式：live-local 官方资料下载与本地 PDF 正文提取；无模型、无 fixture/replay

## 本次真实完成

解决的问题及关联 R 编号：

- 按 AGENTS §11.5 为首批六条 Seed 完成 Level 2 来源准备：实际取得 S24/S26/S28/S29/S30 官方 PDF，核对正文、claim、red flag 与平台边界。
- 修正三处证据归因：Queue 不再把非逐字短语冒充手册引文，并补 `FromISR` 边界；period 的 tick 分辨率改为带来源的技术要点；mutex 删除手册未出现的 `lessen/minimize` 归因。
- 闭合两处平台缺口：UART/DMA 与 SPI/I2C 同时引用 F4/RM0090、G4/RM0440、H7/RM0433；F4 的 I2C 100/400 kHz 与 G4/H7 另列 1 MHz 分开陈述。
- 新增平台来源守卫：两条外设 Seed 的每个技术参考点必须覆盖 F4/G4/H7 已登记来源。
- 审核时发现并修复 live 门禁根因：`SeedBank` 旧实现虽然声称遵循配置，却硬编码把 `technical_review` 当作 live eligible，且 `live_only=True` 返回未批准条目；现读取 `config/demo.yaml` 的 `approved` 门槛并只返回 approved Seed。

修改文件：

- Seed：`data/seeds/*.json` 六条均升到 `0.2.0`；正式状态仍为 `technical_review` / Level 2 `pending`。
- 业务与测试：`services/api/src/zhijue/application/seed_bank.py`、`services/api/tests/unit/test_seed_bank.py`、`tools/validate_spec.py`。
- 审核材料：`docs/level2-review-packet-m2-01.md`、`docs/seeds-review-m2-01.md`、`docs/14-sources-and-rules.md`、`docs/05-knowledge-and-bank.md`。
- 状态同步：`README.md`、`CHANGELOG.md`、`process.md`、`docs/07-test-and-acceptance.md`、`validation-report.md`、`CHECKSUMS.sha256`、本交接。
- ignored 运行证据：`runtime/evidence/m2-01-level2/`（五份官方 PDF、提取文本与 `source-manifest.json`）。

接口/字段/状态/迁移变化：API、DTO、SSE、数据库、迁移、前端、依赖和 Seed Schema 均无变化。`SeedBank` 的既有 `live_only` 行为修正为遵循 `config/demo.yaml` 的 `approved` 门槛并过滤未批准条目；六条 Seed 内容版本从 `0.1.0` 更新为 `0.2.0`，审核状态没有升级。  
有意未修改的内容：不生成首题，不接 LLM，不实现 M3 Analyzer/Policy，不扩到 24 条，不代替负责人作 Level 2 或 approved 决策。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| 五份官方 PDF `sha256sum` | 本地官方快照 | 0 | 5/5 与 `source-manifest.json` 一致 | `runtime/evidence/m2-01-level2/source-manifest.json` |
| `pytest tests/unit/test_seed_bank.py -q` | Python 3.11.16 | 0 | 12 passed；含 F4/G4/H7 来源与 approved-only live 门禁 | 控制台 |
| SeedBank smoke | Python 3.11.16 / `PYTHONPATH=src` | 0 | 6 条加载；指纹 `865acb58c66a8777`；门槛 approved；当前 eligible 0 | `runtime/evidence/m2-01-level2/seed-bank-smoke.txt` |
| `.venv/bin/python -m pytest tests -q` | Python 3.11.16 | 0 | 186 passed / 2 skipped / 39 warnings，13.96s | 控制台 |
| `tools/validate_spec.py` | Python 3.11.16 | 0 | 44/44 passed | `validation-report.md` |
| `ruff check src tests smoke` + `ruff format --check src tests smoke` | Ruff 0.9.10 | 0 | 51 files already formatted | 控制台 |
| `scripts/doctor.py` + `sha256sum -c CHECKSUMS.sha256` | Python 3.11.16 | 0 | doctor 18 pass / 0 warn / 0 fail；完整性清单 157/157 一致 | `CHECKSUMS.sha256` |

实际模型/提示词/题库版本：LLM 与 embedding 均未调用；Seed 六条均为 `0.2.0`，SeedBank 指纹 `865acb58c66a8777`，正式 Level 2 `pending`。  
实际调用数/token/费用：模型调用 0；token/cost 均为 null。  
是否有隐式 mock/回放：无；来源核对使用官方 PDF 的真实下载快照与本地提取文本。  
仍然失败的样本与最小复现：无产品失败。失败尝试保留：① 首轮规范校验因 handoff 尚未创建、process 状态列含非枚举说明而 42/44，修正文档后 44/44；② 首次 SeedBank smoke 漏传 `PYTHONPATH=src` 导致 `ModuleNotFoundError`，补正确运行环境后通过，未增加导入兜底；③ 对历史独立脚本 `tools/validate_spec.py` 直接执行 Ruff 命中其既有 shebang/规则问题，该脚本不在项目标准 Ruff 范围，未借本任务批量重排无关代码。

## 同步与继续

api.md：已检查无变化；本任务不改网络契约。  
process.md：已更新审核准备态、负责人待办和唯一下一任务。  
架构/数据/测试/配置/CHANGELOG：无架构、数据库、配置值或依赖变化；审核文档、来源登记、approved-only 门禁、测试、CHANGELOG 与 validation report 已同步。  
新风险、待负责人事项：负责人或指定独立复核者必须逐条审阅 `docs/level2-review-packet-m2-01.md` 并明确 `passed/failed`；本材料不能自动升级审核状态。  
唯一下一任务：负责人作出六条 Seed 的 Level 2 结论；若通过，再建立可追溯 `review_record_id` 并由负责人明确决定是否 `approved`。  
继续时先运行的命令：`services/api/.venv/bin/python tools/validate_spec.py && cd services/api && .venv/bin/python -m pytest tests -q`  
回滚方式：仅按本交接列出的本轮文件逐项恢复；删除 ignored 的 `runtime/evidence/m2-01-level2/` 不影响业务状态。不得使用 reset、clean、stash 或覆盖连续工作区差异。
