# AI 施工交接：2026-09-18 / M1-03

## 当前状态

工作目录/分支：仓库根 / `master`（仓库尚无 commit）
基线 commit：`NO_COMMITS`；任务前逐文件哈希见 `runtime/evidence/m1-03/task-start.txt`  
本次新增：`services/api/src/zhijue/domain/claims.py`、`adapters/knowledge.py`、`adapters/db/profiles.py`、`application/{profiles,operations_runner}.py`、`api/{app,routes,events,errors,schemas}.py`、`__main__.py`、`tests/unit/{test_profile_confirm,test_knowledge_activation}.py`、`tests/test_api_contract.py`、`contracts/openapi.json`、`Makefile`、`apps/web/src/{api.ts,App.tsx}`、`runtime/evidence/m1-03/`；修改：`smoke/knowledge.py`、`adapters/db/{documents,profiles}.py`、`application/documents.py`、`adapters/pdf.py`、`apps/web/tsconfig.json`、`config/environment.env.example`、`api.md`、process、CHANGELOG、README、docs/07、CHECKSUMS、本文件  
应用版本 / 规范版本：规范 1.0.0；Python 3.11.16；openjiuwen 0.1.18（兼容 commit `72c4985111b835530ec616f70dd67117eb2e015c`）  
当前阶段和任务状态：M1 完成（M1-01/02/03 VERIFIED，本地 + 一次真实 live）；M0-BASE/SPEC-ALIGN BLOCKED；未自行 ACCEPTED  
运行模式：单元与 fixture 级为本地无网络；live 三处：激活链单测、真机 HTTP 闭环（uvicorn + 真实 embedding + Milvus Lite）、真实浏览器闭环（Chromium + vite）。无 LLM 调用

## 本次真实完成

解决的问题及关联 R 编号：docs/12 M1-03（确认/快照/知识激活）；R03 的确认与快照子项、R05 的 Knowledge 激活子项（HTTP/SSE 仍属 M3-02）。  
修改文件：见上。  
接口/字段/状态/迁移变化：HTTP API 无变化（`api.md` §4 的 `/facts`、`/confirm` 契约自始存在，本轮实现与其一致）；数据库无新迁移（Claim/ProfileSnapshot 已在 M1-01 的 14 表中）；新增内部错误语义 `REVISION_CONFLICT`（HTTP 层将映射 409）。  
有意未修改的内容：P-EXTRACT 自动提议 Claim（需模型接线）、HTTP/Operation/SSE 受理、跨进程重启激活复测、前端确认界面（M2-03 工作台一并做）。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `pytest tests/unit/test_profile_confirm.py` | 项目 venv | 0 | 8 passed | 命令输出 |
| `pytest tests/unit/test_knowledge_activation.py` | 项目 venv | 0 | 4 passed、1 skipped（live 未开启） | 命令输出 |
| `pytest tests/unit/test_knowledge_activation.py -m integration_live`（显式私有 env） | 项目 venv + openjiuwen 兼容 commit + Milvus Lite | 0 | 1 passed；`index_status=ready`、回查命中、generation 一致 | `runtime/evidence/m1-03/activation-live-2-*.log` |
| live 去敏汇总 | 同上 | 0 | 1024 维、3 次逻辑 embedding、token/cost null、SDK 未 mock | `runtime/evidence/m1-03/activation-live-summary.json` |
| `.venv/bin/python -m pytest tests -q`（全量） | 项目 venv | 0 | **141 passed / 2 skipped / 0 failed / 39 warnings** | `runtime/evidence/m1-03/pytest-final.log` |
| `pytest tests/test_api_contract.py` | 项目 venv | 0 | 22 passed（契约/SSE/上传/健康/失败路径） | 命令输出 |
| 真机 HTTP 闭环（uvicorn 单进程，live） | Python 3.11 + openjiuwen 兼容 commit + Milvus Lite | 0 | confirm 202→`succeeded`→`index_status=ready`；私有 Demo Resume v1 与 ignored 接收登记一致；过期 revision 409 | 本地 ignored 去敏证据 |
| 真实浏览器闭环（Chromium） | vite dev + live API | 0 | 建档→事实→确认→`ready`，revision 0→1→2 | `runtime/evidence/m1-03/ui-confirm-flow.png` |
| `tsc --noEmit && vite build` | Node 24 + pnpm 锁 | 0 | 构建通过 | 命令输出 |
| Ruff check + format --check | ruff 0.16.8 | 0 | 全通过（smoke/src/tests 34 文件） | 命令输出 |
| 面试链路 E2E（答题/SSE 断流恢复）、重启后激活复测、E2E 用例固化 | — | — | NOT_RUN | 属 M3-02/M2-02 |

实际模型/提示词/题库版本：LLM 未调用（0 次，token/cost null）。embedding provider=openjiuwen_api、model=BAAI/bge-m3、api_origin=https://api.rinko.ai、3 次逻辑调用、计费字段 null。  
是否有隐式 mock/回放：单元测试用显式 `InMemoryKnowledge`（命名即说明非 SDK）；live 用例走真实 SDK，未替换入口。

## 失败与保留

1. live 首次 `RuntimeError: Event loop is closed`：网关 HTTP 连接池绑定创建它的 loop，测试跨 `asyncio.run` 复用。修正=单 loop 驱动整条链（与 uvicorn 单循环一致），未加任何兜底。
2. `smoke/knowledge.py` 收敛构造后 `urlsplit`/`TxtMdParser` 等引用断裂：被既有 16 项守卫测试当场抓住并修复，说明探针回归有效。
3. 未重跑多进程重启后的激活检索：M0-03/M0-03-DEL 已 live 覆盖持久化与重启，本轮复用该结论而非默认通过；如需重启后激活复测，登记为独立任务。
4. 真机缺陷 4 项（本轮修复，均非放宽测试）：终态事件与状态转换顺序反了；后台任务异常逃逸到已发出的响应；运行目录缺失时启动崩溃；`sniff_kind` 未校验文本 UTF-8。前两项在真实 HTTP 首跑即暴露，说明"202 之后无声失败"这类风险确实存在且已被测试覆盖。
5. 前端 `tsconfig` lib ES2022 → ES2024（`Promise.withResolvers` 需要）。

## 同步与继续

额外修复（同轮，校验器自身缺陷）：`tools/validate_spec.py` 扫描边界未排除 `node_modules`/`.venv`/`runtime`/vendored `toolchain/node24`，且含两条过期/写错的断言（PLANNED 规划期基线；写死 `LLM_API_KEY=` 而模板真实变量为 `API_KEY`）。现改为 `iter_assets()` 统一排除 + 施工期状态不变量 + 密钥模板实质检查，**31/31 通过**；`validation-report.md` 已覆盖重写，历史 28/31 记录保留在 process §8。

api.md：补"实现状态"段（已实现路径清单 / 其余仍 PLANNED）；**无契约字段变更**。  
process.md：§1/§2/§4/§22 已更新；M1-03 → VERIFIED（含 HTTP 与界面），下一任务 M2-01。  
架构/数据/测试/配置/CHANGELOG：docs/07 追加 HTTP/SSE/浏览器行；docs/11 runbook 的 `make dev` 现指向真实目标；CHANGELOG/README/CHECKSUMS 同步。  
新风险、待负责人事项：`index_status` 记在 Document 上、快照激活态经由其来源文档体现；若负责人要求"快照自身的激活态"字段，属契约变更需先改 api.md/docs/03。上传解析当前在后台线程内一次性读入内存（≤10 MiB），需要流式落盘时属接口层议题。  
唯一下一任务：M2-01（先 Seed 契约对齐，再落六条审核 Seed）。  
继续时先运行的命令：`make check`（或 `cd services/api && .venv/bin/python -m pytest tests -q`）。  
回滚方式：删除本轮新增路径（domain/claims.py、adapters/knowledge.py、adapters/db/profiles.py、application/{profiles,operations_runner}.py、api/*、__main__.py、两个 unit 测试 + test_api_contract.py、contracts/openapi.json、Makefile、apps/web/src/{api.ts,App.tsx}、runtime/evidence/m1-03/）；`smoke/knowledge.py`、`adapters/db/{documents,profiles}.py`、`application/documents.py`、`adapters/pdf.py`、`apps/web/tsconfig.json`、`config/environment.env.example`、`api.md` 按 `runtime/evidence/m1-03/task-start.txt` 记录的哈希恢复。不使用 reset/stash。
