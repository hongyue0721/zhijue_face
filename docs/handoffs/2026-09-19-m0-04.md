# AI 施工交接：2026-09-18 / M0-04

## 当前状态

工作目录/分支：仓库根 / `master`（仓库尚无 commit）
基线 commit：`NO_COMMITS`；任务前逐文件 hash 见 `runtime/evidence/m0-04/task-start/baseline.txt`  
本次新增：`scripts/doctor.py`、`config/versions.lock.json`、`services/api/tests/test_doctor.py`、`apps/web/`（package.json、tsconfig.json、vite.config.ts、index.html、src/main.tsx、src/App.tsx、pnpm-lock.yaml）、`.github/workflows/ci.yml`；修改：README、CHANGELOG、process、docs/07、config/environment.env.example、CHECKSUMS  
应用版本 / 规范版本：规范 1.0.0；Python 3.11.16；openJiuwen API 0.1.18（兼容 commit `72c49851`）；uv 0.12.10；Node v24.21.0；pnpm 10.34.5  
当前阶段和任务状态：M0 IN_PROGRESS；M0-04 VERIFIED（本地）；M0-BASE BLOCKED；未自行标记 ACCEPTED  
运行模式：doctor/重装/构建为 live-local（无网络、无模型）；模型网关探活为 live 但仅探针；无 fixture/replay

## 本次真实完成

解决的问题及关联 R 编号：M0-04 任务卡（docs/12 §2）：锁版本、契约和最小测试环境 → uv.lock 复验、pnpm-lock、doctor、CI 骨架。R19 的环境可复现子项取得本地证据；不声称 R19 全项。  
修改文件：见上。  
接口/字段/状态/迁移变化：HTTP API、DTO、SSE、幂等、业务数据库迁移、前端类型均无变化；`api.md` 未修改。`config/versions.lock.json` 是新增运行事实清单（非契约）。`config/environment.env.example` 仅注释更新：登记 `deepseek-flash` 网关无通道、`deepseek-v4-flash` 实测可用；`MODEL_API_KEY` 与 embedding key 是否允许共网关由负责人决定，模板语义保持"API_BASE 与 EMBEDDING_API_BASE 分开"。  
有意未修改的内容：业务源码/迁移（M1 才开始）、smoke、契约 Schema、示例题库、旧证据、`tools/validate_spec.py` 历史基线。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `scripts/doctor.py`（锁定 venv） | Python 3.11.16 | 0 | 终态 18 pass / 0 warn / 0 fail；密钥扫描全部 Git 可见文件 0 命中（中途一次 17/1/0，warn 为清单未重建） | `runtime/evidence/m0-04/doctor-final.log` |
| `pytest tests/test_doctor.py -q` | 项目 venv | 0 | 12 passed（首跑 collection ERROR：路径深度差一层，修复后全绿） | 本文件 + 过程记录 |
| `uv venv <tmp> && UV_PROJECT_ENVIRONMENT=<tmp> uv sync --frozen` | uv 0.12.10 | 0 | 184 包；openjiuwen 0.1.18 自 commit `72c49851` 构建 | `runtime/evidence/m0-04/clean-reinstall-sync.log` |
| 新 venv 导入 + SimpleKnowledgeBase 入口 + direct_url 校验 | Python 3.11.16 | 0 | imports OK；commit 与锁一致 | `runtime/evidence/m0-04/clean-reinstall-summary.txt` |
| `pnpm install --frozen-lockfile`（apps/web） | pnpm 10.34.5 / Node 24.21.0 | 0 | lock 一致 | 本地运行输出 |
| `pnpm build`（tsc --noEmit && vite build） | 同上 | 0 | dist 222.72 kB JS | 本地运行输出 |
| `.venv/bin/python -m pytest tests -q`（全量） | 项目 venv | 0 | 62 passed / 1 skipped / 38 warnings | 收尾复跑另见 process §19 |
| `ruff check` + `ruff format --check`（scripts + tests） | ruff 0.16.8 | 0 | 全通过 | 同上 |
| `.github/workflows/ci.yml` YAML 结构核验 | PyYAML | 0 | 两 job 字段完整 | 同上 |
| CI 工作流实际执行 | — | — | NOT_RUN（仓库无远端） | — |

实际模型/提示词/题库版本：Embedding `BAAI/bge-m3`（本轮未调用）；LLM 探针 `deepseek-v4-flash`（temperature 0，单条 8/64 token 合成消息）；Prompt/Seed/Rubric 未使用。  
实际调用数/token/费用：本轮模型调用 = 网关探针 1 次成功 chat（usage 99 tokens）+ 1 次 503 无通道错误 + 1 次列表请求；embedding 0 次；业务链路费用 null。单价未提供，cost 不填 0。  
是否有隐式 mock/回放：无。doctor 守卫测试中的 FakeDistribution 仅替换元数据读取，不触及 SDK 行为。  
仍然失败的样本与最小复现：正式 0.1.18 wheel 的 delete list 缺陷不变（上游风险 K22）；CI 未推送无法执行；`tools/validate_spec.py` 历史 28/31 基线冲突本任务未修。

## 同步与继续

api.md：已检查无变化。  
process.md：§1/§2/§4/§19 已更新；M0-04 → VERIFIED，下一任务 M1-01。  
架构/数据/测试/配置/CHANGELOG：docs/07 追加 M0-04 验证记录；docs/11 doctor 命令状态由"目标契约"更正为"脚本已实现（make 入口待 M1）"；`config/environment.env.example` 注释核验登记；CHANGELOG/README/CHECKSUMS 同步。数据模型与 Workflow/Policy 无实现变化，未修改。  
新风险、待负责人事项：① `MODEL_API_KEY` 与 embedding key 是否共用同一网关账户（影响额度与密钥管理），请负责人在 M2 接线前确认；② 密钥曾经聊天传递，正式配置前建议轮换（沿上轮提醒）；③ 兼容 commit 非官方发布物，官方 release 含 PR #1344 后须切回并重跑 M0-02/M0-03（doctor 与守卫会立即暴露来源漂移）。  
唯一下一任务：M1-01 业务表模型、Alembic 迁移与 Operations 持久化。  
继续时先运行的命令：`cd services/api && .venv/bin/python ../../scripts/doctor.py && .venv/bin/python -m pytest tests -q`。  
回滚方式：依据 `runtime/evidence/m0-04/task-start/baseline.txt` 逐文件恢复；`apps/web`、`scripts/`、`.github/`、`config/versions.lock.json`、`tests/test_doctor.py` 为本轮新增，按清单单独移除即可；不使用 reset/stash，不覆盖用户修改。
