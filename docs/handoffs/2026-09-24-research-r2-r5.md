# AI 施工交接：2026-09-24｜研究分支 R2-R5（Candidate Evidence Fidelity 基础设施）

## 当前状态

工作目录/分支：`/home/hongyue/Projects/zhijue_paper/zhijue_face`，分支 `research/candidate-evidence-fidelity-v1`
基线 commit：`d91023a`（比赛 Demo main）；本分支已提交 `180b9db`（R0）、`75db188`（R1）
本次 commit 或尚未提交的文件：本轮按 R2 / R3+R4 / R5+文档 三个提交合入（见下）
应用版本 / 规范版本：AGENTS 1.0.0；openJiuwen 兼容 commit `72c4985`（非官方发布版）；Python 3.11.16
当前阶段和任务状态：R0-R5 `VERIFIED`（各自登记范围）；正式实验 `PLANNED`，等负责人批准
运行模式：`run_mode=fixture / data_mode=synthetic`；唯一 live 部分是 embedding 单点探测（无对话模型调用）

## 本次真实完成

解决的问题及关联 R 编号：

- R2：研究 harness 骨架（配置/prompt 指纹、数据集可见性投影、四方法 Strategy、scripted 驱动、
  runner + JSON-Schema trace 写盘、离线聚合、CLI）。
- R3：真实 openJiuwen Knowledge 事实级索引与检索、retrieval observer、M2/M3 证据一致性、
  live embedding 单点探测、付费开关守卫。
- R4：5 个确定性检测器 + 8 类 DriftLabel + 双轨指标 + Judge 端口（仅 fixture 实现）。
- R5：3 candidate × 12 case synthetic 数据集与 split、48 cell 全链路 dry run、
  漂移注入自检、泄漏与污染守卫验收、两个度量口径 bug 修复。

修改文件：`research/**`（新增约 29 模块 / 5.0k 行 + 3 契约 + 4 prompt + 配置 + 7 测试文件 +
数据与 split + 探测脚本）；`services/api/src/zhijue/adapters/knowledge.py`（新增默认 `None` 的
`embed_model` 注入位）；`process.md` §63-§66；`CHANGELOG.md`；`research/README.md`；
`research/DESIGN.md`；`CHECKSUMS.sha256`（重建 329 条）。

接口/字段/状态/迁移变化：无。HTTP 字段、错误码、SSE 事件、OpenAPI、数据库 Schema、迁移、
前后端依赖声明均未改动。

有意未修改的内容：比赛 Demo 前端、出题/Policy/评分/Seed Bank、生产 `runtime/`、
`config/experiment.yaml` 的 `embed_mode: fixture` 与 `allow_paid_calls: false`、
dev/test split（留空待冻结）、Judge 的真实模型接入。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `cd research && ../services/api/.venv/bin/python -m pytest tests -q` | Python 3.11.16 / 业务 venv | 0 | 84 passed / 0 failed | `research/tests/` |
| `../services/api/.venv/bin/python -m ruff check src tests scripts` + `ruff format --check` | ruff 0.16.8 | 0 | All checks passed；38 files formatted | — |
| `cd services/api && .venv/bin/python -m pytest tests -q -m 'not integration_live'` | Python 3.11.16 | 1 | 363 passed / 1 failed / 2 deselected | 失败为 `tests/test_doctor.py` 要求本机私密 `.env.local` |
| `services/api/.venv/bin/python tools/validate_spec.py` | 同上 | 0 | 47/47 passed | `validation-report.md`（规范资产静态检查） |
| `cd services/api && .venv/bin/python ../../scripts/doctor.py` | 同上 | 0 | 14 pass / 1 warn / 0 fail | warn 仅 `secret:env.local` |
| `python -m zhijue_research.cli verify-dataset` | PYTHONPATH=src:../services/api/src | 0 | candidates=3 cases=12 fingerprint `5c439fe10641` | `research/splits/pilot.json` |
| `cli run --model-driver scripted --evidence scripted --tag r5-dryrun` | 同上 | 0 | 48 cells，schema 全通过 | `research/runtime/traces/pilot_r5-dryrun.jsonl` |
| `cli run ... --drift-method vanilla --drift-method evidence_bound --tag r5-drift` | 同上 | 0 | M3 acceptance 0.0，`UNBOUND_NUMERIC_FACT ×12` | `research/runtime/traces/pilot_r5-drift.jsonl`、`runtime/reports/r5-drift.json` |
| `cli report --trace runtime/traces/pilot_r5-dryrun.jsonl` | 同上 | 0 | 四臂 raw/accepted 事实性 1.0 | `research/runtime/reports/r5-dryrun.json` |
| `python scripts/live_embedding_probe.py --i-accept-embedding-cost` | 真实 HTTPS embedding | 0 | dim 4096、19 文档、4 次检索、7.516s | `research/runtime/embedding-probe/live-1790275403.json` |
| 正式实验（live 对话模型 × 4 方法 × 全 split） | — | — | NOT_RUN | 需负责人批准 |
| 负责人独立验收 | — | — | NOT_RUN | — |

实际模型/提示词/题库版本：provider `intern-ai-discovery` / model `kimi-k2.6`（本轮**未**调用对话
模型）；embedding `Qwen/Qwen3-VL-Embedding-8B`（仅探测）；prompt 全部 `p1.0`，哈希已钉入
`config/experiment.yaml`；数据集 `cef-1.0.0`，评估器 `cef_eval_1.0.0`，taxonomy 见 `taxonomy.py`。
实际调用数/token/费用：对话模型 0 次；embedding 探测 = 1 次维度探测 + 19 文档批量 + 4 查询，
`cost_cny=null`（上游未回传计费字段，不填 0）。
是否有隐式 mock/回放：无隐式降级。scripted 模型与 scripted 证据都在 trace 里显式标
`model_driver=scripted` / `cost_basis`；`--evidence knowledge` 才会建真实 KB。
仍然失败的样本与最小复现：`services/api/tests/test_doctor.py::test_doctor_real_workspace_passes_and_leaks_no_secret`
在本机必失败——它要求仓库根存在私密 `.env.local`（`git ls-files .env.local` 为 0，从未入库）。
复现：`cd services/api && .venv/bin/python -m pytest tests/test_doctor.py -q`。未用占位密钥伪造通过。

## 同步与继续

api.md：已检查无变更（研究分支不新增网络接口；仅 §62 已登记的 `API_BASE` 值域变化在 R1 同步过）。
process.md：已更新，新增 §63（R2）、§64（R3）、§65（R4）、§66（R5）。
架构/数据/测试/配置/CHANGELOG：`research/DESIGN.md` 阶段表改为实测状态；`research/README.md`
补齐真实命令形态（含 `PYTHONPATH`）；数据与 split 落在 `research/data|splits`；
`CHANGELOG.md` 新增 R2-R5 条目；`CHECKSUMS.sha256` 重建为 329 条一致。
新风险、待负责人事项：

1. 是否批准正式实验：live 模型 × 4 方法 × 12 case 的调用与费用上限。
2. dev/test split 是否现在冻结（`frozen_at` 仍为 null，`case_ids` 为空）。
3. 检索质量需要何种实验面（事实级文档 vs 文档级；单 query vs 多 query）——
   当前 `case_004 recall@6 = 0.2` 是单样本观测，不支持任何结论。
4. 主观标签是否需要第二评审者（`answer_style_rating` 目前只有作者标注）。
5. `tests/test_doctor.py` 的本机私密 env 前提：是补 `.env.local` 还是把该用例改成显式 skip。

唯一下一任务：等负责人对上面 1-2 作决定；批准前不扩数据、不跑付费调用、不做 UI 改动。
继续时先运行的命令：`cd research && ../services/api/.venv/bin/python -m pytest tests -q`
回滚方式：`git revert` 本轮三个提交（R2 / R3+R4 / R5+文档）即可，业务链路只含
`adapters/knowledge.py` 的一个默认 `None` 参数，可单独 revert。
