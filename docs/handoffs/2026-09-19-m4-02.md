# AI 施工交接：2026-09-19 M4-02 事实约束回答优化与简历草稿

## 当前状态

工作目录/分支：`zhijue-demo-spec-v1.0` / `main`  
基线 commit：`4e4e7b43332c7d4e964d58ece3854275ef689a72`  
本次 commit 或尚未提交的文件：本交接与下列代码、迁移、契约、测试和文档由本轮单一提交落盘；最终 SHA 见交付回复  
应用版本 / 规范版本：Demo / 1.0.0  
当前阶段和任务状态：M4-01 `VERIFIED`；M4-02 功能基线 `IMPLEMENTED`；生产内容模型 live、独立验收和两个新页面 Product Polish `NOT_RUN`  
运行模式：浏览器与内容生成闭环为显式 `fixture`；FastAPI、SQLite、Operation 和 openJiuwen Workflow 使用正式实现；外部 Content Generator、embedding 本轮 `NOT_RUN`

## 本次真实完成

解决的问题及关联 R 编号：闭合 R05 的确认后可打印简历、R11/R12 的事实约束回答优化、R13/R14 的单写入 Operation/retry/事件与可追溯持久化。没有把模型改写当成评分证据，也没有把 JD 或缺失项补造成候选人经历。

修改文件：

- 契约：`api.md`、`contracts/{coaching-result,resume-draft-result,operation-event,openapi}.json`。
- 领域与 Workflow：`services/api/src/zhijue/domain/grounded_content.py`、`services/api/src/zhijue/application/{content_workflow,content_generation}.py`、`services/api/src/zhijue/adapters/model.py`。
- 持久化与服务装配：`services/api/src/zhijue/adapters/db/models.py`、`services/api/src/zhijue/application/{interviews,reporting}.py`、`services/api/src/zhijue/api/{app,routes,schemas}.py`、`services/api/migrations/versions/b4d7c2e91f30_m4_02_grounded_content.py`。
- 前端：`apps/web/src/{App,api,routing,storage,styles}.ts*`、`apps/web/src/pages/{InterviewPage,ReportPage,ResumeDraftPage}.tsx`、`apps/web/tests/contracts.test.ts`。
- 测试：`services/api/tests/unit/test_grounded_content.py`、`services/api/tests/{test_content_generation,test_api_contract,test_interview_runtime}.py`。
- 文档：README、CHANGELOG、process、架构、数据模型、Prompt/事实约束、UX、验收、风险、UI Contract、OpenAPI、完整性清单及本交接。

接口/字段/状态/迁移变化：

- `InterviewView` 新增服务端关系字段 `profile_id`。
- 新增 `POST /api/v1/interviews/{id}/report/improvements`；Report 新增 `revision`、`improvements_status`、`active_operation_id` 与结构化 `improved_answers`。
- 新增 `POST /api/v1/profiles/{id}/resume-drafts`、`GET /api/v1/resume-drafts/{id}`、`POST /api/v1/resume-drafts/{id}/accept`。
- 新增 Operation kinds `report.coach` / `resume.compose` 与事件 `coaching.ready` / `resume_draft.ready`；失败通过原 operation 的 `/retry` 建 parent-linked 子操作。单个模型 Operation 只发一次 HTTP 请求，transport/Schema/语义失败统一消耗 `MODEL_MAX_RETRIES + 1` 的累计预算，硬上限三次。
- Alembic head `b4d7c2e91f30` 为 Report 增加生成状态/操作字段并新增 ResumeDraft 表；`(profile_snapshot_id,target_hash)` 唯一。

有意未修改的内容：M3-03 的 Start/Prepare/Interview 三页视觉保持冻结；不改 Policy 四动作、评分器、Assessment、原 Answer、Claim 或 ProfileSnapshot；不新增依赖、OCR、Memory、历史列表、完整简历编辑器或招聘判断。两个新页面只做到清晰可用的功能基线，没有提前做下一轮 Product Polish。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `cd services/api && .venv/bin/python -m pytest tests -q` | Python 3.11 项目 venv；openJiuwen 0.1.18 compatible commit | 0 | 272 passed / 2 skipped / 0 failed / 73 warnings | 本轮命令输出、测试文件 |
| `cd services/api && .venv/bin/ruff format --check src tests smoke migrations && .venv/bin/ruff check src tests smoke migrations` | Ruff 项目锁定环境 | 0 | All checks passed；73 files already formatted | 本轮命令输出 |
| 锁定 Node 24 执行 Vitest、`tsc --noEmit`、Vite build | Node 24.21.0；仓库前端依赖 | 0 | 12/12 passed；TypeScript 通过；114 modules transformed | `apps/web/tests/contracts.test.ts`、本轮命令输出 |
| FastAPI + Vite + Chromium 完成面试→报告→回答优化→简历→确认→打印 | 真实 FastAPI/SQLite/openJiuwen Workflow；ScriptedAnalyzer/ScriptedContentGenerator | 0 | 最终复验显示 `original_answers[].raw_text`、`source_claims[].{id,text}`；print action 调用成功；print media 只保留已确认正文；1440 视口无横向溢出 | `runtime/evidence/m4-02/verification.json`、本轮浏览器截图 |
| `services/api/.venv/bin/python tools/validate_spec.py` | Python 3.11 项目 venv | 0 | 46/46 passed | `validation-report.md` |
| `services/api/.venv/bin/python scripts/doctor.py --json` | Python 3.11 项目 venv | 0 | 18 PASS / 0 WARN / 0 FAIL；223 个 Git 跟踪/待跟踪文件密钥扫描 0 命中 | 本轮命令输出 |
| `sha256sum -c CHECKSUMS.sha256` | 项目根目录 | 0 | 222/222 OK | `CHECKSUMS.sha256` |
| `git diff --check` | 项目根目录 | 0 | 无空白错误 | 本轮命令输出 |

实际模型/提示词/题库版本：外部 Content Generator 本轮未调用；fixture 候选仍经过正式 `Start → Generator → SemanticValidation → End`。真实 Workflow 为 openJiuwen 0.1.18 兼容 commit `72c4985111b8`；Seed/rubric 0.2.1；scoring 1.0.0；coaching/resume result Schema 1.0.0。

实际调用数/token/费用：外部 LLM 0、embedding 0；usage null，cost null。fixture 调用不是付费调用，不用于质量、延迟或成本结论。

是否有隐式 mock/回放：没有隐式替换。浏览器和服务测试显式注入 ScriptedAnalyzer/ScriptedContentGenerator；路由、SQLite、Operation、迁移、真实 openJiuwen Workflow、确定性语义校验与业务写入均为正式实现。

仍然失败的样本与最小复现：没有未修复的 M4-02 回归失败。首次后端全量仅因 OpenAPI 快照在新增接口后过期而 1 failed；重新从实际 FastAPI 导出快照后专项和全量通过。Ruff 首轮暴露三个可合并 context 和一个 import 排序，均按格式器规则修正。浏览器自动化对 AnyUI 的 `role=button` 直接 helper 点击超时，改用实际 DOM click 后验证同一 React handler；打印 hook 的首个跨上下文断言失败，随后在同一页面上下文替换 `window.print` 并证明按钮确实调用，未修改产品代码绕过。

## 同步与继续

api.md：已先行冻结新增端点、Report/Resume 状态、错误、幂等、事件、retry 与确认语义；Python DTO、FastAPI routes、OpenAPI 和前端网络类型已同步。  
process.md：已把 M4-02 记为 `IMPLEMENTED`，未写 `VERIFIED/ACCEPTED`；唯一下一任务切换为两个新页面 Product Polish 与独立验收。  
架构/数据/测试/配置/CHANGELOG：架构、数据模型、Prompt/事实约束、UX、验收、风险、UI Contract、README、CHANGELOG、迁移和完整性清单同步；无新依赖或配置变量。  
新风险、待负责人事项：来源绑定与机械边界不是完整语义真实性证明；用户确认仍是简历打印硬门槛。持续付费调用总上限未提供，生产 Content Generator live 前必须明确 O04；默认 shell 非锁定 Node，前端继续显式使用仓库 Node 24。  
唯一下一任务：只对 `/interviews/:interview_id/report` 和 `/resume-drafts/:draft_id` 做 Product Polish 与独立验收；不返工前三页、不改业务契约、不放宽事实边界。  
继续时先运行的命令：`cd apps/web && ../../toolchain/node24/bin/node node_modules/vitest/vitest.mjs run && ../../toolchain/node24/bin/node node_modules/typescript/bin/tsc --noEmit && ../../toolchain/node24/bin/node node_modules/vite/bin/vite.js build`。  
回滚方式：以基线 `4e4e7b43332c7d4e964d58ece3854275ef689a72` 为文件级参照，恢复本交接列出的已有文件并删除本轮新增 schema、domain/application 模块、迁移、测试、页面和 handoff；不使用 reset/stash，不覆盖后续用户修改。
