# AI 施工交接：2026-09-18｜M0-02 局部验证与约束补充

## 当前状态

- 工作目录：项目根；分支 master；基线 commit：无（尚无首次提交）。
- 本次 commit：无；未 commit/push，未 reset/stash/清理用户改动。
- 应用版本：尚无业务应用；smoke 1.0.0；规范 1.0.0 + 负责人 2026-09-18 补充约束。
- M0-01：IMPLEMENTED（审计与风险登记；独立验收未做）。
- M0-02：IMPLEMENTED（局部回归通过；独立验收未做）。M0 整体未通过。
- M0-BASE：BLOCKED（官方答疑/补充基座要求尚未完整核实）。SPEC-ALIGN：BLOCKED（Policy/Seed 语义冲突）。ADR-011：PROPOSED。
- 模式：live，仅本地真实框架；synthetic=true。不是 live 模型；Knowledge/LLM NOT_RUN。

## 本次真实完成

关联 R19 的框架调用子项，不代表全部 R19 或 P0 验收。

### 架构与真实执行

目标架构仍是轻量前端 → 单 Python 业务后端 → 官方 Agent/Workflow/Knowledge；Python 是唯一业务写入者。前端、业务服务与数据库尚未实现。

本次真正执行：

```text
合成 hello → Workflow.invoke（官方 workflow session）→ Start → TransformText → End
合成 hello → WorkflowAgent.invoke → legacy ControllerAgent.invoke
           → WorkflowController → Runner.run_workflow_streaming → Workflow
           → Start → TransformText → End
结果：{"response":"R=HELLO|tagged"}
```

运行时继承链为官方 WorkflowAgent → 官方 legacy ControllerAgent → 官方 legacy BaseAgent → ABC → object；不是新版 BaseAgent 的独立验证，更不代表官方比赛专用宿主已确认。绝对源码路径与 hash、MRO 见 smoke-a.json。

自定义的只是纯文本节点；SDK Agent/Workflow 没有替换，Knowledge 没有假接入。测试注入的抛错/等待节点不是 Demo 业务实现。

### 修改文件与原因

| 文件 | 原因 |
|---|---|
| AGENTS.md | 固化新增 20 条强制约束及保守冲突处理 |
| services/api/smoke/__init__.py | 使用官方日志配置 API 只写控制台，避免运行日志混入源码 |
| services/api/smoke/workflow.py | 两条真实 SDK 入口、严格结果检查、超时清理、证据 CLI、源码/MRO/hash |
| services/api/tests/conftest.py | 在 SDK 测试导入前配置独立探针日志 |
| services/api/tests/test_workflow_smoke.py | 34 项正常/非法输入/错误/超时/恢复/重复/hash/防覆盖用例 |
| services/api/README.md | 当前实际可执行命令与局部验证边界 |
| docs/adr/011-workflow-smoke-scope.md | 记录无模型局部实验选择；从自行标 ACCEPTED 改回 PROPOSED |
| docs/02-architecture.md | 区分规划拓扑与实际 SDK 继承/调用链 |
| docs/07-test-and-acceptance.md | 记录已测范围、失败与恢复、未测范围 |
| docs/13-risks-and-decisions.md | 补 Lite/embedding/legacy/官方基座及 Policy/Seed 冲突 |
| docs/14-sources-and-rules.md | 官方 u-j8/答疑读取边界、SDK 来源与许可证 |
| process.md / CHANGELOG.md | 任务、真实状态、证据、补充规范与下一步 |
| docs/handoffs/2026-09-18-m0-02.md | 本交接 |
| CHECKSUMS.sha256 | 同步上述规范变化并纳入新增探针/交接及后端 README，不声称覆盖私有运行资产 |

原始日志、快照、官方页面、执行记录及校验辅助脚本在 runtime/evidence/m0-02/，被既有 .gitignore 排除。SDK 默认产生的本轮 logs 仅迁移至该目录，未删除用户内容。

有意未修改：api.md、contracts/、examples/、config/、docs/03-data-model.md、tools/validate_spec.py、pyproject.toml、uv.lock；旧代码/工具链保持原状。未新增依赖、HTTP 接口、字段、状态、事件、数据库迁移或业务 DTO。

## 验证证据

环境：Python 3.11.16 / openjiuwen 0.1.18 / pytest 9.1.1 / Ruff 0.16.8；Linux 7.2.3-arch1-2 x86_64 glibc 2.44。不是国产 OS 适配证明。

下列命令 cwd 为 services/api；最终轮时间为 **2026-09-18T07:54:45-07:00 至 07:54:59-07:00**。完整 argv、cwd、开始/结束、返回码见 commands.jsonl。

| 实际命令/用例 | 退出码 | 结果 | 证据（runtime/evidence/m0-02/ 下） |
|---|---:|---|---|
| `.venv/bin/python -m pytest tests -q`（首次红灯） | 2 | 1 个收集错误，缺 smoke.workflow | red.log |
| 同命令（首次实现） | 1 | 10 通过 / 5 失败 | first-run.log |
| 同命令（第一次基于证据修复） | 0 | 原 15 项通过 | second-run.log |
| `.venv/bin/python -m pytest tests -q --junitxml=<tests-final.xml>` | 0 | **34 通过 / 0 失败；38 条警告** | pytest-final.log、tests-final.xml |
| `.venv/bin/ruff check smoke tests` | 0 | 通过；首次 5 项问题保留在 ruff.log | ruff-final.log |
| `.venv/bin/ruff format --check smoke tests` | 0 | 4 文件格式通过 | format-final.log |
| `.venv/bin/python -m smoke.workflow --output <smoke-a.json>` | 0 | 两入口结果精确匹配 | smoke-a.json、smoke-a.log |
| 同命令，另一进程输出 smoke-b.json | 0 | 两入口再次通过，输出 hash 一致 | smoke-b.json、smoke-b.log |

34 项由 **7 个真实 SDK 执行用例 + 27 个输入/结果守卫用例**组成。没有替换 SDK 入口；守卫测试构造异常 WorkflowOutput，仅测试拒绝规则，不假称真实框架异常输出。

输出 hash 为 `a16f7569d77bdf402de0c9b051d81f24bd82e3e3e2e5fedef6b01a5fb8114b94`，定义为 outputs 的排序键/无空白/UTF-8 JSON SHA256。整份证据含时间和路径，不要求整文件 hash 相同。

### 失败原因与修复依据

- 首次 Workflow 调用漏传必需 session，按 0.1.18 源码补官方 create_workflow_session。
- 首次 Agent 超时后等待节点未在 2 秒内取消；按 SDK task manager 接口增加调用级任务组，清理后重抛原始错误，保留同一超时断言。不是改小输出/吞异常/绕开框架。
- 正常重复执行是新图/session 无状态输出一致；失败恢复是新执行成功，不是业务幂等、断电恢复或持久化会话恢复。
- SDK legacy/Pydantic/DashScope 弃用警告未屏蔽，保留升级风险；没有为消除警告擅自换依赖。

### 尚未运行

Knowledge 入库/检索/重启/删除、实际模型、前后端/E2E、数据库、业务幂等与恢复、干净环境重装、独立验收均 NOT_RUN。

`tools/validate_spec.py` 本轮 NOT_RUN；前轮 28/31 及三项已知基线冲突保留，不覆盖 validation-report.md，不擅自修校验器。聚焦的资产检查命令（仓库根）：`services/api/.venv/bin/python runtime/evidence/m0-02/check_assets.py`，exit 0：5 个 Schema、8 个示例、14 个本轮文本文件 UTF-8/LF/本地链接通过，未授权修改的原始规范资产 hash 不变。`sha256sum -c CHECKSUMS.sha256` exit 0：62/62 通过。结果见 asset-final.log / checksum-final.log，时间和返回码见 commands-assets.jsonl；不冒充旧全仓库校验器通过。

实际 provider/model/prompt/seed/rubric：null；模型调用 0；token/费用 null（没有计费响应，不假填 0）。质量/延迟等业务指标 NOT_MEASURED。没有隐式回放或 mock 成功。

## 未解决问题

1. **M0-BASE**：已取得 NCSS 题面及 GitCode u-j8 详细解读；已读页面未列专用 starter/BaseAgent。答疑区只取得应用 HTML 壳，无帖子正文、无可用浏览器，不能据此排除额外通知。原页面及 HTTP/hash 在 official/。最小解阻是官方答疑正文、模板仓库或基座书面说明；如另有宿主，接入现有能力后再实测，不另建平行 Agent。
2. **Policy 契约冲突**：新增条款含 CHALLENGE，旧 docs/04 与两个 Schema 仅四动作，且校验器明确拒绝 CHALLENGE。按保守原则暂不扩枚举；先确认独立动作语义/预算/状态，再串行同步 api.md → Schema/类型/调用方/测试/process。不能把现状说成五动作已支持。
3. **Seed 契约缺口**：已有 rubric/followup_intents/reference_ids 不自动等于 reference_points/red_flags/strategy；M2-01 前补契约或经审核的语义映射，不批准/批量扩充当前 draft Seed。
4. **Knowledge**：Lite db_name 风险和本地 embedding 缺口仍未通过原生链路验证；不改后端、不加云服务。
5. **O04 等外部条件**：真实模型配置/预算、审核来源及学校/国产 OS 口径仍按 process.md 保留。

## 同步与继续

- api.md：已检查，无接口变更；冲突显式登记，不静默改枚举/Schema。
- process.md / AGENTS / 架构 / 测试 / 来源 / 风险 / CHANGELOG：已同步。
- 数据说明、配置、锁文件：未改，无持久化或依赖变化。
- 阅读版 HTML、validation-report.md：保留最初规范发布快照，不当作当前工程进度。
- **唯一下一任务：M0-BASE**。当前阶段先汇报并停止，未自动进入 M0-03/M1。
- 继续前先运行 `git status --short` 并读 process.md；复跑测试使用上表命令；复跑 smoke 必须用新输出文件名。
- 回滚：原文档快照在 runtime/evidence/m0-02/baseline/；追加约束前局部源码/文档快照在 constraint-checkpoint/。先比较负责人后续改动，只撤销本轮增量；新文件按本表单独处理。不得重置整个工作区；用户新增约束不得随源码回退而丢失。
