# AI 施工交接：2026-09-19 / M0-03

## 当前状态

工作目录/分支：`zhijue-demo-spec-v1.0` / `master`  
基线 commit：仓库尚无 commit（`No commits yet on master`）  
本次 commit 或尚未提交的文件：未 commit、未 push；工作区原有文件均为 untracked，本轮没有 reset/stash/覆盖用户修改  
应用版本 / 规范版本：规范 1.0.0；Python 3.11.16；openjiuwen 0.1.18  
当前阶段和任务状态：M0-03 `BLOCKED`；不是 VERIFIED/ACCEPTED  
运行模式：`live` 框架/embedding + `synthetic` 输入；DeepSeek LLM `NOT_RUN`

> live 命令在本地时区 2026-09-18 执行；本交接于 2026-09-19 汇总。

## 本次真实完成

解决的问题及关联 R 编号：

- 真实确认 BAAI/bge-m3 可通过 openJiuwen SDK 自带 `OpenAIEmbedding` 返回 1024 维向量。
- 真实运行 `SimpleKnowledgeBase → TxtMdParser → CharChunker → MilvusIndexer/MilvusVectorStore → Milvus Lite`。
- 两个 synthetic profile 完成解析、入库、dense 检索、document/chunk/source/hash provenance、KB 隔离与跨进程重启。
- 缺失源文件与重复 doc_id 不会报成功；证据输出拒绝 key 与绝对服务器路径。
- 收敛删除 blocker：openJiuwen 0.1.18 无法处理 pymilvus 删除成功时返回的 primary-key list；与官方 PR #1344 同根因。

修改文件：

- 代码/测试：`services/api/smoke/knowledge.py`、`services/api/tests/test_knowledge_smoke.py`、`services/api/pyproject.toml`。
- 配置：`config/demo.yaml`、`config/environment.env.example`；私密 `.env.local` 为 0600 且 Git ignored，不纳入交付。
- 文档：`docs/02-architecture.md`、`docs/05-knowledge-and-bank.md`、`docs/07-test-and-acceptance.md`、`docs/13-risks-and-decisions.md`、`docs/14-sources-and-rules.md`、`docs/adr/012-native-knowledge-local-compatibility.md`、`docs/demo/README.md`、`services/api/README.md`、`process.md`、`CHANGELOG.md`、本交接、`CHECKSUMS.sha256`。
- 本地证据：`runtime/evidence/m0-03/`、`runtime/m0-03-live-*`，全部 Git ignored。

接口/字段/状态/迁移变化：

- `api.md` 无变化，SHA256 仍为 `7d85d7fc8abfe56bb75cd87516274800770775d4c416f7e6746a4034f3ff5cb0`。
- 无 HTTP 路径、DTO、SSE 事件、领域 Schema、数据库迁移或前端类型变化。
- 配置模板改为负责人冻结的 `MODEL_PROVIDER / MODEL_NAME / API_BASE / API_KEY / MODEL_TIMEOUT / MODEL_MAX_RETRIES`，另设独立 `EMBEDDING_*`，不共享 key。

有意未修改的内容：

- 未修改 `.venv/site-packages`，未 monkeypatch 或复制 openJiuwen Knowledge。
- 未实现 Evidence/JD Requirements/Candidate State/Seed/Planner/Analyzer/Policy/UI。
- 未发送 Demo Resume v1 给外部模型；仅发送两段显式 synthetic 文本。
- 未调用 DeepSeek；未处理 CHALLENGE/Seed 契约冲突；未进入 M0-04/M1。
- 未运行会改写报告且有历史 28/31 基线冲突的 `tools/validate_spec.py`。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `pytest tests/test_knowledge_smoke.py -q`（实现前） | Python 3.11 | 2 | 预期红测：缺 `smoke.knowledge` | `runtime/evidence/m0-03/knowledge-lifecycle-red.log` |
| Knowledge 单测/守卫最终回归 | Python 3.11 | 0 | 13 passed / 1 skipped；skip 为显式 live 用例 | 命令输出；最终由全量 pytest 覆盖 |
| live attempt `T171000` | openJiuwen 0.1.18 | 1 | `/embeddings` 2xx 非 JSON，SDK 包装失败 | `runtime/evidence/m0-03/knowledge-live-20260918T171000.log` |
| live diagnostic `T171500` | 同上 | 1 | 确认异常链为 JSONDecodeError | `runtime/evidence/m0-03/knowledge-live-20260918T171500.log` |
| live final `T172000` | BGE-M3 + Milvus Lite | 1 | ingest/restart 通过；delete 阶段失败，post-delete NOT_RUN | `runtime/m0-03-live-20260918T172000/phase-*.json`、对应 log |
| 去敏部分证据汇总 | Python 3.11 | 0 | `status=BLOCKED`、1024 维、7 次成功逻辑 embedding 调用 | `runtime/evidence/m0-03/knowledge-partial-20260918T172000.json` |
| pymilvus 集合/返回形态取证 | pymilvus 2.6.7 | 0 | profile A rows=0、B rows=1；delete 可返回 list/OmitZeroDict | 本轮控制台记录；ADR-012 摘要 |
| `.venv/bin/python -m pytest tests -q` | Python 3.11.16 | 0 | 47 passed / 1 skipped / 0 failed / 38 warnings，8.12s | `runtime/evidence/m0-03/pytest-final.log` |
| `ruff check smoke tests` + `ruff format --check smoke tests` | ruff 0.16.8 | 0 | 全通过；6 files formatted | `runtime/evidence/m0-03/ruff-final.log` |
| YAML、UTF-8/LF、API hash 检查 | 项目 venv | 0 | YAML 通过；34 个目标文件通过；API hash 未变 | 本轮控制台记录 |
| key/权限/Git ignore/真实简历标记扫描 | 本地 | 0 | key 泄漏 0；`.env.local` 0600/ignored；Knowledge fixture 无真实简历标记 | 本轮控制台记录 |
| `sha256sum -c CHECKSUMS.sha256` | 本地 | 0 | 72/72 通过；新增 smoke、handoff、pyproject/uv.lock 已纳入 | `runtime/evidence/m0-03/checksums-final.log` |

实际模型/提示词/题库版本：

- Embedding provider：负责人授权的 OpenAI-compatible gateway；SDK 类 `openjiuwen...OpenAIEmbedding`。
- 请求模型：`BAAI/bge-m3`；实际返回维度 1024。网关内部映射未获得独立厂商证明，不扩大结论。
- LLM：`deepseek-flash` 仅官方模型名已核对，项目 live 调用 `NOT_RUN`。
- Prompt / Seed / Rubric / Policy：`null / NOT_RUN`。

实际调用数/token/费用：

- 成功逻辑 embedding 调用：7（ingest 5、restart 2）。前两次错误路径各发起一次 HTTP 请求，但没有证据证明到达模型服务。
- OpenAI SDK 内部实际 transport 尝试数：`NOT_MEASURED`；最大 retry 有界为 1。
- usage/token/cost：SDK 未暴露，均为 `null`；不能写 0。
- DeepSeek LLM 调用：0 次实际调用；五题 Demo 成本 `NOT_MEASURED`。

是否有隐式 mock/回放：无。普通单测使用合成输入和配置守卫；live 主链没有替换 SDK 入口。  
仍然失败的样本与最小复现：调用 `SimpleKnowledgeBase.delete_documents([doc_id])`，pymilvus 返回被删主键 list，openJiuwen 0.1.18 `MilvusIndexer.delete_index` 执行 `int(list)`，返回 false。官方 PR #1344 描述同一缺陷。

## 同步与继续

api.md：已检查无变更；hash 与本轮前清单一致。  
process.md：已更新为 M0 / BLOCKED，M0-03 / BLOCKED。  
架构/数据/测试/配置/CHANGELOG：架构、Knowledge、测试、风险、来源、ADR、Demo 登记、服务 README、配置与 CHANGELOG 已同步；数据模型/Workflow Policy 没有实现变化，未修改。  
新风险、待负责人事项：

1. 优先等待/采用包含官方 PR #1344 的正式 openJiuwen 发布版。
2. 若赛期不允许等待，需要负责人明确批准按官方补丁语义实现窄 compatibility adapter；仍不得改 site-packages 或假装原生版本无缺陷。
3. 此前密钥曾通过聊天传递；最终比赛配置前应轮换，并继续只放本地 secret。

唯一下一任务：`M0-03-DEL`，解除删除兼容并重跑完整生命周期；不得先进入 Evidence/JD/Seed/UI。  
继续时先运行的命令：先查官方 PR #1344 是否已进入正式发布版；不要重复当前 live 命令消耗额度。  
回滚方式：本轮前快照在 `runtime/evidence/m0-03/baseline-20260918T170600/`；逐文件比对后恢复，不使用 reset/stash，不覆盖后续用户修改。`.env.local` 属私密本地配置，不进入 Git/清单。
