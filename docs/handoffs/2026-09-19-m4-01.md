# AI 施工交接：2026-09-19 M4-01 确定性评分与报告

## 当前状态

工作目录/分支：`zhijue-demo-spec-v1.0` / `main`
基线 commit：`a74b6a37e99bf7850b7b5dabbbcafc31096739e0`
本次 commit 或尚未提交的文件：本交接与下列代码、迁移、契约、测试和文档由本轮单一提交落盘；最终 SHA 见交付回复
应用版本 / 规范版本：Demo / 1.0.0
当前阶段和任务状态：M4-01 `VERIFIED`；M3 全部 `VERIFIED`；前三页 UI `FROZEN`；M4-02 `PLANNED`
运行模式：后端评分/报告烟测为 `fixture`；真实 openJiuwen Workflow、FastAPI、SQLite 和业务持久化未替换；外部模型与 embedding 本轮 `NOT_RUN`

## 本次真实完成

解决的问题及关联 R 编号：闭合 R09 的 skip/end 服务端控制、R11 的 Rubric 反馈与 UNKNOWN/null 边界、R13 的单份评分/报告和失败恢复、R14 的持久化 Decision/Operation/report 事件。M4-01 不包含 R12 回答优化。

修改文件：

- 契约与客户端：`api.md`、`contracts/openapi.json`、`apps/web/src/api.ts`。
- 评分与报告：`services/api/src/zhijue/domain/scoring.py`、`services/api/src/zhijue/application/reporting.py`、`services/api/src/zhijue/application/interviews.py`。
- API/装配/恢复：`services/api/src/zhijue/api/{app,errors,routes,schemas}.py`、`services/api/src/zhijue/adapters/db/{models,operations}.py`。
- 迁移：`services/api/migrations/versions/7f1b9c4d2a60_m4_01_report_uniqueness.py`。
- 测试：`services/api/tests/unit/{test_scoring,test_migrations,test_operations_repository}.py`、`services/api/tests/{test_api_contract,test_interview_runtime}.py`。
- 文档：根与服务 README、`CHANGELOG.md`、`process.md`、`docs/{02-architecture,03-data-model,04-workflow-policy,06-prompts-and-factuality,07-test-and-acceptance,13-risks-and-decisions,ui-contract}.md`、`validation-report.md`、`CHECKSUMS.sha256`、本文件。

接口/字段/状态/迁移变化：新增 `POST /api/v1/interviews/{id}/control`（`skip/end`）与 `GET /api/v1/interviews/{id}/report`；新增 `REPORT_NOT_READY`；Report 精确返回 completion、coverage、五个 root assessments、nullable score、空 improved_answers、limitations、run metadata。`Decision.observation_id` 的网络类型允许控制操作使用 null。Alembic head 增加 Assessment `(interview_id,root_question_id)` 和 Report `interview_id` 唯一约束。进程恢复把 queued/running 都标 interrupted，因为内存 callable 没有持久化。

有意未修改的内容：不新增第五个 Policy 动作，不让模型计算数字，不实现 M4-02 improved answer/Resume Draft，不新增依赖，不修改冻结三页视觉，也不伪造 Report UI、真实模型整场、429/timeout、p95 或费用结果。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `PYTHONPATH=src .venv/bin/python -m pytest tests/unit/test_scoring.py -q`（实现前） | Python 3.11 项目 venv | 2 | `ModuleNotFoundError: zhijue.domain.scoring`；预期红灯 | 本轮命令输出 |
| 同命令（实现后） | Python 3.11 项目 venv | 0 | 5 passed | `services/api/tests/unit/test_scoring.py` |
| `.venv/bin/python -m pytest tests/unit/test_scoring.py tests/test_api_contract.py tests/test_interview_runtime.py tests/unit/test_migrations.py -q` | Python 3.11 项目 venv | 0 | 52 passed / 21 warnings | 对应测试文件 |
| `.venv/bin/ruff format --check src tests smoke migrations && .venv/bin/ruff check src tests smoke migrations && .venv/bin/python -m pytest tests -q` | Python 3.11 项目 venv | 0 | 67 files；261 passed / 2 skipped / 0 failed / 63 warnings | 本轮命令输出 |
| 临时 TestClient 五题烟测 | 真实 FastAPI/openJiuwen Workflow/SQLite；显式 ScriptedAnalyzer fixture | 0 | complete；5 scored roots；overall_score=67；status=completed；临时脚本已删除 | 本轮命令输出 |
| `services/api/.venv/bin/python tools/validate_spec.py` | 项目 venv | 0 | 44/44 passed | `validation-report.md` |
| 显式使用仓库 `toolchain/node24/bin` 后执行 `corepack pnpm@10.34.5 test` | Node v24.21.0 / pnpm 10.34.5 | 0 | 11/11 passed | `apps/web/tests/contracts.test.ts` |
| 同一锁定环境执行 `corepack pnpm@10.34.5 build` | Node v24.21.0 / pnpm 10.34.5 | 0 | TypeScript + Vite，112 modules | 本轮命令输出 |
| `services/api/.venv/bin/python scripts/doctor.py --json` | Python 3.11 项目 venv | 0 | 18 PASS / 0 WARN / 0 FAIL；212 个 Git 跟踪文件密钥扫描 0 命中 | 本轮命令输出 |
| `sha256sum -c CHECKSUMS.sha256` | 项目根目录 | 0 | 211/211 OK | `CHECKSUMS.sha256` |
| `git diff --check` | 项目根目录 | 0 | 无空白错误 | 本轮命令输出 |

实际模型/提示词/题库版本：本轮没有外部模型调用；烟测使用显式 ScriptedAnalyzer fixture。真实 Workflow 仍为 openJiuwen 0.1.18 兼容 commit `72c4985111b8`；Seed/rubric 0.2.1；Policy 1.0.0；scoring 1.0.0。

实际调用数/token/费用：外部模型调用 0、embedding 调用 0；fixture Analyzer 进程内调用 5 次。没有外部 token usage；cost 未发生，不能把 fixture 次数写成付费调用。

是否有隐式 mock/回放：没有隐式替换；烟测明确为 fixture Analyzer。FastAPI 路由、真实 openJiuwen Workflow、SQLite、Operation、Observation、Decision、Assessment、Report 与事件均运行正式实现。

仍然失败的样本与最小复现：没有未修复的 M4-01 回归失败。首轮 Ruff import 排序失败已按格式器修正；首次生成 OpenAPI 时 SDK 日志混入 stdout 导致 JSON `Extra data`，改为直接写临时 JSON 后更新。默认 Node 26 验证带 engine warning，且 pnpm 的 `--use-node-version` 尝试被拒绝；doctor 随后定位仓库 Node 24，最终锁定工具链测试/构建通过。Report UI、真实模型五题浏览器整场、真实 429/timeout、p95 和费用 benchmark 未运行。

## 同步与继续

api.md：已先行更新 control/report、错误、幂等、评分和恢复契约；Python DTO、FastAPI routes、OpenAPI 与前端网络类型已同步。
process.md：已更新 M4-01 为 `VERIFIED`，把唯一下一任务切换为 M4-02。
架构/数据/测试/配置/CHANGELOG：架构、数据模型、Workflow、评分、验收、UI Contract、风险、README、CHANGELOG、迁移和完整性清单已同步；无新依赖和配置项。
新风险、待负责人事项：确定性评分不能修复错误 Observation；Report UI 与真实模型整场仍未证明。持续付费调用总上限仍未提供，M4-02 若要调用外部模型必须先明确预算。默认 PATH 是 Node 26，后续前端验证应继续显式使用仓库 Node 24.21.0。
唯一下一任务：M4-02 事实约束的回答优化与简历草稿入口；先同步 API，保持每个实质句子可回查确认事实，不把缺失项补造成用户经历。
继续时先运行的命令：`cd services/api && .venv/bin/python -m pytest tests/unit/test_scoring.py tests/test_interview_runtime.py -q`。
回滚方式：以基线 `a74b6a37e99bf7850b7b5dabbbcafc31096739e0` 为文件级参照，恢复本交接列出的已有文件并删除本轮新增 scoring/reporting/migration/test/handoff；不使用 reset/stash，不覆盖后续用户修改。
