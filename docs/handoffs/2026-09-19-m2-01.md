# AI 施工交接：2026-09-18 / M2-01

## 当前状态

工作目录/分支：仓库根 / `master`
基线 commit：`7ac1b7f`（本轮落盘后的本地提交；落盘前为 0 commit，任务前逐文件哈希见 `runtime/evidence/m2-01/task-start.txt`）  
本次新增：`docs/adr/013-seed-contract-alignment.md`、`docs/seeds-review-m2-01.md`、`data/seeds/*.json`（6 条）、`services/api/src/zhijue/application/seed_bank.py`、`services/api/tests/unit/test_seed_bank.py`、`runtime/evidence/m2-01/`  
修改：`contracts/seed.schema.json`、`examples/seed_*.json`（3）、`tools/validate_spec.py`、`docs/04-workflow-policy.md`、`docs/05-knowledge-and-bank.md`、`docs/14-sources-and-rules.md`、`docs/07`、`README.md`、`CHANGELOG.md`、`process.md`、`validation-report.md`、`CHECKSUMS.sha256`、本文件  
当前阶段和任务状态：M2 IN_PROGRESS；**M2-01 IMPLEMENTED**（不是 VERIFIED：Level 2 审核未完成、负责人未确认）；M0-BASE 仍 BLOCKED  
运行模式：全程本地文件与官方文档阅读；无网络模型调用、无 LLM、无 embedding（0 次，token/cost null）

## 本次真实完成

解决的问题：**SPEC-ALIGN**（CHALLENGE 与 Seed 新增语义未对齐）与 M2-01 首批六条种子。  
关联需求：docs/12 M2-01；AGENTS §11.4/§11.5/§11.7/§11.8；docs/05 §6/§8。  
契约变更（`api.md` 无变化；`contracts/` 有变化）：

- `contracts/seed.schema.json` 新增必填 `reference_points`、`red_flags`、`follow_up_strategy`、`review_levels`，并加 4 条条件约束。
- `contracts/policy-decision.schema.json` **不变**（保持四动作）；CHALLENGE 语义由 ADR-013 记录为 `PROBE + counterfactual`。
- `examples/seed_*.json` 三个示例按新契约补全。

有意未修改：Policy/事件的 CHALLENGE 枚举；`api.md`；数据库模型与迁移；种子数量（不扩到 24 条）；`data/knowledge/` 来源清单（已在 docs/14 登记映射）。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `python tools/validate_spec.py` | 项目 venv（jsonschema 4.26 / PyYAML 6.0.3） | 0 | **37/37 通过**（新增 5 条 Schema 负例 + 种子可追溯性） | `validation-report.md` |
| 负向自检：种子 `reference_ids` 改为不存在 id | 同上 | 1 | 校验器报 `FAIL 种子引用可追溯`（证明非空断言） | 命令输出 |
| `pytest tests/unit/test_seed_bank.py` | 项目 venv | 0 | 8 passed（live 门槛、坏契约、空目录、版本指纹） | 命令输出 |
| `pytest tests -q`（全量） | 项目 venv | 0 | **149 passed / 2 skipped / 0 failed / 39 warnings** | 命令输出 |
| Ruff check + format --check | ruff 0.16.8 | 0 | 全绿 | 命令输出 |
| FreeRTOS 官方手册核对 | 网络只读，官方 PDF | 0 | 读到版次 V10.0.0 issue 1 与 Ch2/Ch3/Ch4/Ch7 结构 | `docs/14` S24 登记 |
| ST RM0433 核对 | 网络只读，官方 PDF 40,711,860 B | 0 | 第 15/19/47/48/50/56 章与 19.1/47.4/48.5.6/48.5.19/50.4/56.4 实测存在 | `docs/14` S26 登记 |
| **Level 2 技术审核** | — | — | **NOT_RUN（待负责人/独立复核者）** | `docs/seeds-review-m2-01.md` |
| LLM / embedding / 浏览器 E2E | — | — | NOT_RUN | — |

实际模型/提示词/题库版本：无模型调用。种子库版本指纹由 `SeedBank.version_fingerprint()` 计算（Interview.seed_bank_version 将使用它）。  
是否有隐式 mock/回放：无。

## 失败与保留

1. 委派的两个只读来源核验子代理（scout）**因 API 配额 429 全部失败**（`Your token-plan 1-week quota has been exhausted`）。未重试等待，改为本机自行下载官方文档核对——结果更可靠（拿到 PDF 版次与章节号），该失败如实保留。
2. freeertos docs 站为客户端渲染，本机只取到标题与元数据；因此正文细节引用**官方参考手册 PDF（S24）**而非在线页，边界已在 S25 登记。

## 同步与继续

api.md：无变化（本轮不动 HTTP 契约）。  
process.md：§1/§2/§4/§23 已更新；M2-01 → IMPLEMENTED，下一任务 M2-02。  
CHANGELOG/README/docs/07/CHECKSUMS/validation-report：已同步。  
ADR-013：状态 **PROPOSED**，未标记 ACCEPTED；待负责人确认后才可把种子推进到 `approved`。  
新风险、待负责人事项：

- **需要负责人决定两项**：① ADR-013 是否确认（尤其"保持四动作、CHALLENGE 作为 PROBE 子意图"这一取舍）；② Level 2 技术审核由谁做（负责人本人对照 S24/S26，或指定独立复核者）。这两项未决前，种子不得 approved、不得扩到 24 条。
- `rm0433_stm32h7` 只覆盖 STM32H7；后续若涉及 F1/F4 系列必须新增对应手册登记。

唯一下一任务：M2-02（JD 输入与来源状态持久化、能力覆盖计划、五题计划生成）。  
继续时先运行的命令：`make check`（或 `cd services/api && .venv/bin/python -m pytest tests -q`），再 `services/api/.venv/bin/python tools/validate_spec.py`。  
回滚方式：见 process.md §4 的回滚点段（删除本轮新增路径 + 按 `runtime/evidence/m2-01/task-start.txt` 哈希恢复改动文件）。不使用 reset/stash。
