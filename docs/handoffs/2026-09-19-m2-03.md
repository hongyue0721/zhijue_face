# AI 施工交接：2026-09-19 M2-03

## 当前状态

工作目录/分支：仓库根 / `master`
基线 commit：`7ac1b7f`  
本次 commit 或尚未提交的文件：尚未提交；保留 M2-01、M2-02、来源纠错与 M2-03 的连续工作区差异，未覆盖或清理既有内容  
应用版本 / 规范版本：`0.1.0` / `1.0.0`  
当前阶段和任务状态：M2 / IN_PROGRESS；M2-03 VERIFIED；M2-01 Level 2 仍 pending  
运行模式：live（浏览器、FastAPI、SQLite、Milvus Lite、openJiuwen Knowledge）

## 本次真实完成

解决的问题及关联 R 编号：

- R03/R06/R08/R14：把资料确认快照、JD 来源、能力覆盖关系和五槽位计划放入同一个可恢复工作台；不生成题目文本。
- 来源审查发现并删除不可验证的 `demo-jd-v2.md`；撤回 `REAL_JD_DERIVED` 宣称。默认 Demo JD 现在固定为 `synthetic_demo_jd`，用户粘贴文本固定为 `user_provided`。
- `real_jd_derived` 新增强制 provenance 门禁：缺上游 URL、抓取时间、上游 hash、派生 hash、转换说明，或使用本地路径/示例域名，一律 `JD_PROVENANCE_INVALID`。
- 浏览器显著展示“不是企业真实招聘公告”；unknown 显示“材料未体现，不等于不会”；related_context 与直接证据分开计数。
- Seed 仍为 `technical_review`、Slot `seed_id=null`；首题显示“审核门禁中/尚未生成”，没有回答入口，也没有 LLM 调用。

修改文件：

- API/领域：`api.md`、`contracts/openapi.json`、`services/api/src/zhijue/api/schemas.py`、`routes.py`、`application/interviews.py`、`application/requisition.py`、`domain/requisition.py`。
- 测试/Live：`services/api/tests/test_api_contract.py`、`tests/unit/test_planning.py`、`smoke/m2_02_live_verification.py`。
- Web：`apps/web/src/api.ts`、`App.tsx`、`index.html`、`package.json`、`vite.config.ts`。
- 事实同步：`docs/demo/demo-jd-v1.md`、`docs/demo/README.md`、M2-02/M2-03 handoff、`process.md`、`CHANGELOG.md`、`CHECKSUMS.sha256`。

接口/字段/状态/迁移变化：

- `POST /interviews` 删除客户端 `jd_source_type`：不提供 `jd_text` 时由服务端加载合成预置；提供时来源固定为 `user_provided`。
- `InterviewView.jd_source` 返回直接来源与上游/派生来源字段，synthetic 场景所有 URL 为 null、`derived=false`。
- 无数据库迁移；Interview 已有 JSON 快照字段承载新来源字段。

有意未修改的内容：

- 不实现 `/start`、回答提交、Analyzer、Policy、评分或报告；这些属于 M3/M4。
- 不把六条 Seed 改为 approved，不扩到 24 条，不绕过 Level 2。
- 不新增第三方依赖。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `services/api/.venv/bin/python services/api/smoke/m2_02_live_verification.py` | Python 3.11.16 / openJiuwen 0.1.18 compat / BAAI/bge-m3 / SQLite / Milvus Lite | 0 | 21 facts、8 requirements、5 slots、8/8 关键事实检查；synthetic=true、derived=false、URL=null | 本地 ignored 去敏证据，不随公开仓库发布 |
| `.venv/bin/python -m pytest tests -q` | Python 3.11.16 | 0 | 182 passed / 2 skipped / 39 warnings | 控制台，11.06s |
| `.venv/bin/ruff check ...` + `ruff format --check ...` | ruff 0.9.10 | 0 | 通过；51 files formatted | 控制台 |
| `tools/validate_spec.py` | Python 3.11.16 | 0 | 43/43 passed | `validation-report.md` |
| pinned Node 24 `tsc --noEmit` + `vite build` | Node v24 toolchain / TypeScript 5.9 / Vite 7.3.6 | 0 | 29 modules；JS 233.38 kB / gzip 73.91 kB | `apps/web/dist/`（ignored build output） |
| 实际浏览器：打开 Profile/Interview → 点击生成计划 → 等待 Operation → 刷新恢复 | Chromium / live API / Vite proxy | 0 | Operation `operation_a5690d32777560d56c06` succeeded；Interview `interview_46d8ee8bc76efccb75df`；5 Slots、synthetic 警示、unknown 边界、首题门禁均可见；刷新后恢复 | `runtime/evidence/m2-03/workbench-live-final.png` |
| `services/api/.venv/bin/python scripts/doctor.py` | Python 3.11.16 | 0 | 18 pass / 0 warn / 0 fail | 控制台 |
| 完整性清单逐文件 SHA256 复核 | Python 3 | 0 | 156/156 一致 | `CHECKSUMS.sha256` |

实际模型/提示词/题库版本：LLM 未调用；Embedding `BAAI/bge-m3` 仅用于最终 M2-02 Knowledge 激活；SeedBank 指纹 `076d0fa846398905`，未绑定任何 Seed。  
实际调用数/token/费用：最终 M2-02 live 对 21 个事实块执行向量化；SDK 未暴露 token/费用，均为 null；M2-03 计划与 UI 的 LLM 调用为 0。  
是否有隐式 mock/回放：浏览器验收无 mock/replay，使用真实本地 HTTP、SQLite 与 live Knowledge；常规 pytest 中保留既有测试替身，但不作为 live 证据。  
仍然失败的样本与最小复现：首次 M2-02 纠错重跑使用错误环境变量名，readiness 失败并退出 1；改用 `ZHIJUE_MILVUS_URI` 与 `ZHIJUE_EMBEDDING_ENV_FILE` 后通过，未给业务代码增加兜底。

## 同步与继续

api.md：已先更新来源类型和 `InterviewView.jd_source` 契约，再修改实现；`contracts/openapi.json` 已由 FastAPI 重新导出，含 Interview 路径且请求中不再有 `jd_source_type`。  
process.md：已更新 M2-02 来源纠错、最终 live 证据及 M2-03 浏览器结果。  
架构/数据/测试/配置/CHANGELOG：架构保持单后端/单数据库写入方；无迁移、无新依赖；测试和 CHANGELOG 已同步。  
新风险、待负责人事项：六条 Seed Level 2 仍 pending；UART/DMA 与 SPI/I2C 的平台适用来源缺口仍未获负责人审核，不得进入 live question。  
唯一下一任务：负责人或指定独立复核者完成 M2-01 Level 2，并明确 ADR-013/Seed 批准结论；施工 Agent 不自行写 approved。  
继续时先运行的命令：`cd services/api && .venv/bin/python -m pytest tests -q`  
回滚方式：按本交接“修改文件”逐项恢复到基线 `7ac1b7f`；不得使用 reset、clean、stash 或覆盖未提交内容。
