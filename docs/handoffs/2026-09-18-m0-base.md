# AI 施工交接：2026-09-18｜M0-BASE 官方基座核验

## 当前状态

- 工作区：项目根；分支 master；基线 commit：无，尚无首次提交。
- 本次 commit：无；保留原有未提交文件，无 reset/stash/force push。
- 应用：尚无业务应用；规范 1.0.0 + 负责人补充约束；现有 smoke 1.0.0。
- **M0-BASE：BLOCKED**。本轮加固了 SDK 和公开资料证据，但官方答疑正文仍无法取得，不标记“基座要求全部确认”。
- M0-02 仍 IMPLEMENTED，独立验收 NOT_RUN；M0 整体未通过。Knowledge/LLM/业务闭环 NOT_RUN。
- 运行模式：live，仅本地框架真实执行，输入 synthetic；资料核验不属于业务 Web Fetch。

## 本次真实完成

### 当前架构与真实链路

目标架构不变：轻量前端 → 唯一 Python 业务后端 → 官方 Agent/Workflow/Knowledge；Python 独占核心业务状态写入。前端、业务 HTTP 服务、数据库链路尚未实现。

复跑的是现有最小图，不是面试业务：

```text
Workflow.invoke + 官方 create_workflow_session → Start → TransformText → End
WorkflowAgent.invoke → 官方 legacy ControllerAgent → WorkflowController
                     → Runner.run_workflow_streaming → Workflow → 同一三节点图
```

输入为合成 hello，严格得到 `{"response":"R=HELLO|tagged"}`。实际 MRO：WorkflowAgent → legacy ControllerAgent → legacy BaseAgent → ABC → object。没有另造 Agent；自定义纯文本节点不是模型/Knowledge 的替身。

### 证据结论（R19 的框架子项，不是全部验收）

1. **实际版本与包来源可核对。** 安装版与 uv.lock 均为 openjiuwen 0.1.18；重新下载的锁定 wheel SHA256 为 `b5fdc79cfa121324b473d9002d5c8be7514b3932f8866e22a1723147004d6d8a`，与锁一致。只读取 ZIP，没有重新安装。7 个被审计的入口/控制器/Workflow/session/Runner 文件全部与 wheel 逐字节一致；前轮源码 hash 未变化。
2. **官方类与调用接口有对应源码/API 文档。** 固定 commit `6dfda012193d09dc3908524d5f8ae406811d909f` 的 WorkflowAgent 源文件与安装版一致；但 7 个文件中仅 5 个与该 commit 相同，Workflow 与 session 的 2 个文件不同。发布 wheel 比对证明这是版本差异，不应伪称“整个 develop 与安装版一致”。差异包括网页仓库新增 name/trace_id 参数，施工继续以锁定发布版为准。
3. **找到了官方通用示例，但不是已确认的比赛 starter。** `examples/workflow_agent/build_workflow_agent.ipynb` 是天气查询教学示例，含模型/外部服务和旧 `openjiuwen.agent.*` 导入。只测试该旧导入，按预期报 ModuleNotFoundError；未执行 notebook 或获取模型/天气凭据。没有为了跑旧示例换 SDK。
4. **公开检索有范围，不能证明全局不存在。** 官方 GitHub 组织列表返回 20 个仓库；读取 agent-core/docs/community 的目录树，均 truncated=false；对命题/starter/面试/Agent 相关路径筛查后读取相关文件。未定位本题专用强制 starter，但未全文审计所有仓库，更未获得 GitCode 答疑全文，不能宣称官方没有额外要求。
5. **官方答疑是具体外部阻塞。** 从公开前端资源确认了只读讨论列表接口；2026-09-18T08:28:40-07:00 匿名只读 POST 查询返回 **HTTP 418 CloudWAF**，没有讨论正文。另一个公开 GitHub 组织 discussions 入口返回 404。均保留原始响应，不登录、不绕过 WAF/验证码、不将失败解释为空列表。

### 修改文件与原因

| 文件 | 修改原因 |
|---|---|
| process.md | 领取/续接、证据结论、回到 BLOCKED、停止条件与外部解阻动作 |
| docs/02-architecture.md | 记录已证实调用范围及发布版/仓库差异，架构不变 |
| docs/13-risks-and-decisions.md | 更新 K19 官方答疑阻塞及 K03/K17 版本证据 |
| docs/14-sources-and-rules.md | 增加 S16—S19 官方源码/API/示例/仓库/wheel 来源及限制 |
| CHANGELOG.md | 本轮已发生事实，不列未实现功能 |
| docs/handoffs/2026-09-18-m0-base.md | 本交接 |
| CHECKSUMS.sha256 | 同步上述文本，并加入新交接文件 |

runtime/evidence/m0-base/：新增只读核验脚本、原始 HTTP 响应、源码/wheel 快照、差异、审计 JSON、运行日志与回滚快照，按既有规则忽略。下载源码/包只作本地核验，不进入业务代码或对外材料。

**有意未修改：** api.md、AGENTS.md、services/api 下源码/测试/README/依赖锁、contracts/、examples/、config/、迁移、前端、旧仓库、旧校验器。API/字段/状态/事件/数据库/依赖均无变化；没有新增第三方依赖或产品能力。

## 验证证据

环境：Python 3.11.16 / openjiuwen 0.1.18 / pytest 9.1.1；Linux 7.2.3-arch1-2 x86_64 glibc 2.44。不是国产 OS 适配证明。

前三项 cwd 为 services/api，实测时间 **2026-09-18T08:31:21-07:00 至 08:31:37-07:00**；完整 argv、cwd、时刻、返回码见 runtime/evidence/m0-base/commands.jsonl。

| 实际命令/检查 | 退出码或 HTTP 状态 | 结果 | 证据（runtime/evidence/m0-base/ 下） |
|---|---:|---|---|
| `.venv/bin/python -c "import runpy; runpy.run_path('../../runtime/evidence/m0-base/audit_sdk.py', run_name='__main__')"` | 0 | 锁/安装版本相同；官方入口文件相同；7 文件中 5 个与仓库 commit 一致、2 个差异如实记录 | sdk-audit.json、sdk-audit.log |
| `.venv/bin/python -m pytest tests -q --junitxml=<tests.xml>` | 0 | **34 passed / 0 failed / 38 warnings**，沿用原用例，无删改断言 | tests.xml、pytest.log |
| `.venv/bin/python -m smoke.workflow --output <smoke.json>` | 0 | 两条真实入口输出严格匹配 | smoke.json、smoke.log |
| 仓库根 `services/api/.venv/bin/python runtime/evidence/m0-base/audit_wheel.py` | 0 | 锁定 wheel hash 一致，审计文件 7/7 与发布包一致；未安装 | wheel-audit.json、wheel-audit.log |
| 该审计中的 `python -c 'import openjiuwen.agent.workflow_agent.workflow_agent'` | **1（预期）** | ModuleNotFoundError；旧 notebook 路径不适用本版，未跑完整 notebook | official-example-import.log、wheel-audit.json |
| 官方讨论列表只读查询 | **HTTP 418** | 获取失败，不是“0 条讨论” | public/discussions-page-1.json（实际为拦截 HTML）、fetch.jsonl |
| 公开 GitHub 组织 discussions | **HTTP 404** | 未获得内容，不作不存在补充要求的证据 | public/github-discussions.html、fetch.jsonl |

获取脚本保存错误体时可正常退出；以 fetch.jsonl 的 HTTP status 为准，不能把脚本 exit 0 当作接口成功。wheel 审计首次执行起始时刻未单独记录，不补造；JSON 的 checked_at 与旧导入检查的时间单列保存。

smoke 输出 SHA256：`a16f7569d77bdf402de0c9b051d81f24bd82e3e3e2e5fedef6b01a5fb8114b94`，与前轮相同。它只哈希规范化 outputs，不等于整个证据文件 hash。

本轮没有修改被测源码，因此未新增应用测试或运行 Ruff；干净环境重装/独立验收/Knowledge/模型/E2E/数据库与持久恢复均 NOT_RUN。旧 tools/validate_spec.py 本轮 NOT_RUN，前轮三项已知问题保留，未覆盖原报告。

实际模型调用 0；provider/model/token/费用/prompt/seed/rubric null；业务 Grounding Rate/延迟等 NOT_MEASURED。没有隐式 mock/回放成功。

## 同步与继续

- api.md：已检查无变化；业务 Schema/DTO/调用方无改动。
- process、架构、风险、来源、CHANGELOG、本交接：已同步。
- 数据说明/配置/锁/原测试文档：无变化，原验证范围未扩大。
- 仍未解决：官方答疑/补充模板是否另有宿主要求；Knowledge Lite/embedding 组合；O04 模型配置；Policy/Seed 契约对齐；学校/国产 OS 口径。
- **唯一下一动作：M0-BASE 外部解阻。** 需要负责人提供本题官方答疑/补充材料、指定模板仓库或命题方关于 Base Agent 的说明（链接、正文或截图均可），或正常访问恢复后复核。不要反复盲查同一被拦接口；不要用选手仓库代替命题方要求。
- 当前阶段汇报后停止；没有领取 M0-03 或进入 M1，没有自行 ACCEPTED。
- 继续时先读 process.md 并运行 `git status --short`；收到官方材料后先审核内容和来源，若确有指定宿主，再在该宿主实测接入现有能力，不另造 Agent。
- 回滚：本轮原件在 runtime/evidence/m0-base/baseline/；只比对并撤销本轮文档增量/新交接，不覆盖后续用户编辑，不清理旧代码/证据，不执行 destructive git。


## 本轮文档收尾校验

仓库根执行 `services/api/.venv/bin/python runtime/evidence/m0-base/check_documents.py`，exit 0：6 个文本文件 UTF-8/LF/本地链接通过，上一完整性清单中非本轮修改的 57 个文件 hash 未变，API 与现有 smoke 源码未改，任务仍为 BLOCKED。

执行 `sha256sum -c CHECKSUMS.sha256`，exit 0：新增本轮交接后 **63/63** 文件一致。时间、命令与返回码见 runtime/evidence/m0-base/commands-docs.jsonl，输出为 document-check.log / checksum-check.log。该检查不等于业务或全仓库规范校验通过。
