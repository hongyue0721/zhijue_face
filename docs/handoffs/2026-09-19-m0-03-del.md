# AI 施工交接：2026-09-19 / M0-03-DEL

## 当前状态

工作目录/分支：仓库根 / `master`（仓库尚无 commit）
基线 commit：`NO_COMMITS`；任务前逐文件 hash 见 `runtime/evidence/m0-03-del/task-start-20260918T175700.txt`  
本次 commit 或尚未提交的文件：业务仓库未 commit/push；修改 `.gitignore`、`README.md`、`CHANGELOG.md`、`CHECKSUMS.sha256`、`config/demo.yaml`、`process.md`、`services/api/{README.md,pyproject.toml,uv.lock,tests/test_knowledge_smoke.py}`、`docs/{02,05,07,13,14}`、ADR-012 与本交接  
应用版本 / 规范版本：规范 1.0.0；Python 3.11.16；openJiuwen API 版本 0.1.18；兼容 commit `72c4985111b835530ec616f70dd67117eb2e015c`  
当前阶段和任务状态：M0 IN_PROGRESS；M0-03 / M0-03-DEL VERIFIED；M0-BASE BLOCKED；未自行标记 ACCEPTED  
运行模式：live + synthetic（Knowledge）；fixture/mock 只用于依赖返回形态单测，不用于 live 主链

## 本次真实完成

解决的问题及关联 R 编号：解除 R19/M0-03 中 Milvus Lite 删除返回 list 导致 openJiuwen 0.1.18 `int(list)` 失败的 blocker。  
修改文件：

- 依赖与测试：`services/api/pyproject.toml`、`services/api/uv.lock`、`services/api/tests/test_knowledge_smoke.py`。
- 配置与忽略：`config/demo.yaml`、`.gitignore`。
- 文档：根 `README.md`、`docs/02-architecture.md`、`docs/05-knowledge-and-bank.md`、`docs/07-test-and-acceptance.md`、`docs/11-runbook.md`、`docs/13-risks-and-decisions.md`、`docs/14-sources-and-rules.md`、`docs/demo/README.md`、`docs/adr/012-native-knowledge-local-compatibility.md`、`services/api/README.md`、`CHANGELOG.md`、`process.md`、本文件。
- 上游隔离工作区：官方仓库 clone/worktree 位于业务仓库外；创建公开 fork `hongyue0721/agent-core` 的 `compat/v0.1.18-milvuslite-delete` 分支。没有把上游源码复制进业务仓库。

接口/字段/状态/迁移变化：HTTP API、DTO、SSE、幂等、业务数据库迁移、前端类型均无变化；`api.md` SHA256 仍为 `7d85d7fc8abfe56bb75cd87516274800770775d4c416f7e6746a4034f3ff5cb0`。配置新增 openJiuwen 兼容来源/commit/上游 PR 状态字段，并将 Knowledge 删除兼容标记为真实验证通过。  
有意未修改的内容：Evidence、Candidate State、Resume→Evidence、JD→Requirements、六 Seed、Planner、Analyzer/Policy、评分、UI、DeepSeek LLM 均未开始；Demo Resume v1 未外发；没有修 `tools/validate_spec.py` 的历史基线冲突。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| 正式 v0.1.18 list 删除回归 | Python 3.11.16 / v0.1.18 tag | 1 | 1 failed / 1 passed；确认 `int(list)` | `runtime/evidence/m0-03-del/baseline-regression.log` |
| 同回归 + PR #1344 精确补丁 | Python 3.11.16 | 0 | 2 passed | `runtime/evidence/m0-03-del/patched-regression.log` |
| 上游 `test_milvus_indexer.py` | v0.1.18 + 两补丁提交 | 0 | 21 passed / 0 failed | `runtime/evidence/m0-03-del/upstream-targeted-tests.log` |
| 补丁 worktree 四进程 Knowledge live | BGE-M3 / Milvus Lite | 0 | parse/add/retrieve/restart/delete/post-delete 全通过 | `runtime/evidence/m0-03-del/knowledge-patched-live-20260919T010300Z.json` |
| `uv lock` + `uv sync --frozen` | uv 0.12.10 | 0 | openjiuwen 0.1.18 从固定 commit 构建安装 | `runtime/evidence/m0-03-del/uv-lock-compat.log`、`uv-sync-frozen-compat.log` |
| 锁定安装、`env -u PYTHONPATH` 四进程 live | Python 3.11.16 / pymilvus 2.6.7 / milvus-lite 3.2.1 | 0 | 4 个不同 PID；两 profile 删除后总命中 0 | `runtime/evidence/m0-03-del/knowledge-locked-live-20260919T011100Z.json` |
| Knowledge 兼容守卫 | 项目 venv | 0 | 16 passed / 1 skipped | `runtime/evidence/m0-03-del/test-knowledge-smoke-compat.log` |
| `.venv/bin/python -m pytest tests -q` | 项目 venv | 0 | 50 passed / 1 skipped / 0 failed / 38 warnings，8.52s | `runtime/evidence/m0-03-del/pytest-final.log` |
| Ruff check + format check | ruff 0.16.8 | 0 | 全通过，6 files already formatted | `runtime/evidence/m0-03-del/ruff-final.log` |
| `uv lock --check` + direct_url commit guard | uv / 项目 venv | 0 | 194 包锁有效；安装 commit 与请求 revision 一致 | `runtime/evidence/m0-03-del/lock-source-final.log` |
| YAML/JSON/UTF-8/LF/Markdown 本地链接 | Python 3.11 | 0 | 73 个文本文件、17 个 JSON、42 个 Markdown 检查通过 | `runtime/evidence/m0-03-del/docs-update-validation.log` |
| 密钥/权限/Git ignore/compileall | 本地 | 0 | key 命中 0；`.env.local` 0600；runtime/logs ignored | `runtime/evidence/m0-03-del/docs-update-security.log` |
| `sha256sum -c CHECKSUMS.sha256` | 本地 | 0 | 74/74 通过 | `runtime/evidence/m0-03-del/checksums-final.log` |
| 上游 PR 反馈 | GitHub / `hongyue0721` | 0 | 已向现有 PR #1344 发布 live 验证；未重复开 PR | `runtime/evidence/m0-03-del/upstream-pr-1344-published-comment.json` |
| `tools/validate_spec.py` | — | — | NOT_RUN；历史 28/31 冲突不属于本任务 | 既有 process 记录 |

实际模型/提示词/题库版本：Embedding 由 openJiuwen `OpenAIEmbedding` 请求 `BAAI/bge-m3`，实测 1024 维；网关内部映射未独立证明。LLM / Prompt / Seed / Rubric / Policy：NOT_RUN / null。  
实际调用数/token/费用：本阶段两次 live 各 9 次成功逻辑 embedding，共 18 次；M0-03 累计 25 次。HTTP transport 尝试数 NOT_MEASURED；usage/token/cost 均为 null；五题 Demo 成本 NOT_MEASURED。  
是否有隐式 mock/回放：无。依赖回归单测显式 mock `MilvusClient.delete()` 的返回形态；两次 live 未替换 openJiuwen Knowledge、Indexer、Store 或 embedding 入口。  
仍然失败的样本与最小复现：正式 v0.1.18 wheel 的非空 list 删除仍失败；兼容 commit 通过。live 日志存在 dense-only 集合缺少 `sparse_vector` 的 BM25 fallback warning，但 dense 结果与全部断言通过；没有声称 sparse/BM25 已验证。

失败尝试：第一次 `gh repo fork` 误用不支持的 `--remote=false`，exit 1；按 CLI 帮助改用 `--clone=false` 后成功。一次 shell hash 比对命令括号/管道语法错误，exit 2；修正后源码 hash 一致。失败未被覆盖成成功，也未删除断言。 最终安全/状态检查首轮的宽泛正则误命中 `task-start` 路径及“未 ACCEPTED”否定说明，两个检查 exit 1；收紧边界和正向状态条件后均 exit 0，真实密钥扫描仍为 0。

## 同步与继续

api.md：已检查无变化；hash 如上。  
process.md：已更新 M0-03 / M0-03-DEL 为 VERIFIED，M0 总体仍 IN_PROGRESS，M0-BASE 仍 BLOCKED。  
架构/数据/测试/配置/CHANGELOG：入口 README、Demo 登记、runbook、架构、Knowledge、测试、风险、来源、ADR、服务说明、配置与 CHANGELOG 已同步；数据模型和 Workflow/Policy 没有实现变化，未修改。  
新风险、待负责人事项：正式 openJiuwen release 尚未包含修复；当前首次依赖同步要求 Git/网络，兼容 commit 不是官方签名发布物。官方 release 含修复后必须切回正式 wheel 并重跑 M0-02/M0-03。此前密钥经聊天传递，最终比赛前仍建议轮换。  
唯一下一任务：M0-04 实测版本锁与 doctor；本轮按阶段停止规则不自动开始。  
继续时先运行的命令：`cd services/api && uv lock --check && .venv/bin/python -m pytest tests -q`，随后读取 `process.md` 的 M0-04 范围。  
回滚方式：依据 `runtime/evidence/m0-03-del/task-start-20260918T175700.txt` 逐文件恢复；依赖可改回 `openjiuwen==0.1.18` 并用保存的 `uv.before-compat.lock` 对照，但不得使用 reset/stash 覆盖后续修改。兼容 fork/上游评论属于公开外部记录，不通过删除历史伪装未发生。
