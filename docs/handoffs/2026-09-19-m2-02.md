# AI 施工交接：2026-09-19 M2-02 JD 来源持久化、Requirement 抽取与五题计划生成（事实性与溯源闭环）

## 当前状态

工作目录/分支：仓库根 / `master`
基线 commit：`7ac1b7f`（首个提交）  
应用版本 / 规范版本：`0.1.0` / `1.0.0`  
当前阶段和任务状态：M2 / IN_PROGRESS（M2-02 状态：VERIFIED）  
运行模式：live（真实 openJiuwen Knowledge + Milvus Lite + SQLite 驱动）+ demo_input（真实 Demo Resume v1 PDF + 显式 `SYNTHETIC_DEMO_JD`）

## 本次真实完成（事实性与溯源闭环）

1. **P0 事实性排查与“DMA 双缓冲”根除**：
   - 排查事实：生产存证与数据库中绝无“双缓冲”；该词来自早期测试用例中的 mock 文本，在人工汇报摘要中不慎残留。
   - 根因闭合：修正单元测试 fixture，新增负向回归测试 `test_negative_regression_ungrounded_fact_rejected`，强制校验 Candidate Claim 必须是 SourceBlock 的精准子串；任何未在原文中声明的词汇均被抛出 `ValueError` 拦截。
2. **P0 中断 Evidence Mapping 纠偏与 EvidenceRelation 体系**：
   - 查明事实：Demo Resume v1 全文对“中断/NVIC/ISR/EXTI”的直接出现频次为 **0 次**；此前将 TIM/输入捕获误当直接证据属于分类失真。
   - 建立四级 `EvidenceRelation` 架构：`DIRECT_CLAIM`、`DIRECT_EXPERIENCE`、`RELATED_CONTEXT`、`MODEL_INFERENCE`；
   - 强制不变量：`RELATED_CONTEXT` 严禁将 unknown 升级为 unverified；在 Coverage Map 中 `embedded.mcu.interrupt` 严格保持 `status=unknown`、`relation=related_context`、`evidence_ids=[]`、`related_context_ids=['claim_tim_xxx', ...]`。
3. **JD 来源真实性纠错与强制门禁**：
   - 审查发现此前 `demo-jd-v2.md` 使用不可验证示例域名并写入未经记录的 `confirmed_by`；已删除该工件并撤回 `REAL_JD_DERIVED` 宣称；
   - 默认 Demo JD 固定来自 `data/jd/preset_embedded_junior.txt`，返回 `source_type=synthetic_demo_jd`、`is_synthetic=true`、所有 URL 为空；客户端提供文本固定为 `user_provided`，不能自报真实来源类型；
   - 领域层新增 `JD_PROVENANCE_INVALID` 门禁：真实衍生 JD 缺少上游 URL/抓取时间/hash/派生 hash/转换说明，或使用本地路径、示例域名，均直接失败。
4. **Planner 多维度业务优先级（消除单纯字母序 tie）**：
   - 废除 32 同分依赖字典序的缺陷；优先级公式考量四重业务要素：
     `priority = 岗位重要度(10/20/30) + 验证需求与证据关系分值(2-8) + JD需求覆盖密度(0-6) + 证据丰富度(0-3)`；
   - 来源纠错后的 live 计划梯度：软硬件验证(40) > UART/DMA(39) = ownership(39) > 中断(38，unknown+高信息价值) > C基础(37)；字典序仅作为完全并列时的稳定兜底。
5. **Demo Critical Fact Checklist 客观召回率**：
   - 废除主观绝对表述，改为客观可测量的 8 项检查集（STM32、UART错帧排查、FreeRTOS、Queue、CAN、Git、主要负责人、Linux学习边界）；
   - 实测召回率：**8/8（100.0%）**。
6. **Level 2 状态与并行边界明确**：
   - UART/DMA 与 SPI/I2C 在补齐 F407/G431 官方来源前继续禁止进入 live question；
   - M2-03 允许并行开展资料确认、Evidence 展示、Coverage Map 展示与 5 Slot Plan 展示的只读 UI 部分，严格锁定首题实时出题。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `services/api/.venv/bin/python services/api/smoke/m2_02_live_verification.py` | Linux Python 3.11.16, SQLite, Milvus Lite, BAAI/bge-m3, 显式私有 Demo Resume v1 + 合成 Demo JD | 0 | 通过；21 facts、8 requirements、5 slots、8/8 关键事实检查；synthetic/derived=false/URL=null | 本地 ignored 去敏证据，不随公开仓库发布 |
| `.venv/bin/python -m pytest tests -q` | Linux Python 3.11.16 | 0 | 182 passed / 2 skipped / 0 failed / 39 warnings | 控制台输出，耗时 11.06s |
| `services/api/.venv/bin/python tools/validate_spec.py` | Python 3.11.16 | 0 | 43/43 passed / 0 failed | `validation-report.md` |
| `services/api/.venv/bin/python scripts/doctor.py` | Python 3.11.16 | 0 | 18 pass / 0 warn / 0 fail | 控制台输出 |
| `.venv/bin/ruff check src tests smoke` | ruff 0.9.10 | 0 | All checks passed | 控制台输出 |
| `.venv/bin/ruff format --check src tests smoke` | ruff 0.9.10 | 0 | 51 files already formatted | 控制台输出 |

实际模型/提示词/题库版本：  
- Embedding 模型：`BAAI/bge-m3`（经真实 openJiuwen API 调用，1024 维）  
- 题库版本指纹：`076d0fa846398905`（M2-02 严禁绑定未批准种子，槽位 `seed_id=null`）  
- LLM 调用：0 次（P0 规划阶段纯程序规则驱动）  

实际调用数/token/费用：  
- Embedding 调用：21 次逻辑块向量化入库（切分 21 块）  
- Token / 费用：SDK 未暴露，计费字段记为 null  

是否有隐式 mock/回放：否，全流程使用真实 SQLite、真实 Milvus Lite、真实 openJiuwen SDK 链路与真实本地 HTTP 服务。  

首次 live 尝试因命令使用了错误变量名 `ZHIJUE_KNOWLEDGE_URI` / `ZHIJUE_ENV_FILE`，readiness 返回非 data 错误体并退出 1；改用契约中的 `ZHIJUE_MILVUS_URI` / `ZHIJUE_EMBEDDING_ENV_FILE` 后重新创建空 runtime 并通过。业务实现无需为该操作错误增加兜底。

## 同步与继续

api.md：已检查并更新（同步补齐 `InterviewView` 中 `jd_source` / `coverage_map` 字段及 `POST /interviews` 参数）  
process.md：已更新（第 25 节完整追加 M2-02 Closure 闭环事实）  
CHANGELOG.md：已更新（M2-02 升级为 VERIFIED 并记录细节）  
新风险、待负责人事项：  
- 6 条 Seed 目前处于 `technical_review` 状态，UART+DMA 与 SPI/I2C 在补齐 F407/G431 官方来源前禁止进入 Demo live。  
唯一下一任务：M2-03 当前题工作台（前端界面只读承载资料确认快照、五题规划展示与首题就绪交互）。  
继续时先运行的命令：`cd services/api && .venv/bin/python -m pytest tests -q`  
回滚方式：以基线 commit `7ac1b7f` 和本交接列出的路径逐项恢复；不得使用 `git clean`、reset 或覆盖负责人未提交内容。
