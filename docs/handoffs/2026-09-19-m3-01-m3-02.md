# AI 施工交接：2026-09-19 M3-01 / M3-02

## 当前状态

工作目录/分支：`zhijue-demo-spec-v1.0` / `master`  
基线 commit：`7ac1b7f`（初始提交；后续 M2/M3 工作仍在用户工作区）  
本次 commit 或尚未提交的文件：本轮未 commit、未 push；修改与新增见下方完整清单  
应用版本 / 规范版本：Demo / 1.0.0  
当前阶段和任务状态：M3-01 `IMPLEMENTED`（业务模型 live `NOT_RUN`）；M3-02 本地后端 `VERIFIED`；M3-03 `PLANNED`  
运行模式：fixture；外部文本模型 `NOT_RUN`

## 本次真实完成

解决的问题及关联 R 编号：M3-01 回答结构化分析、语义校验、有限 Policy，以及按选中 criterion/冻结 Rubric 聚焦且不暴露内部 ID 的确定性 PROBE 文案；M3-02 幂等受理（含并发相同回答单写入）、durable event、失败保留、retry 与重启恢复。
修改文件：

- 领域/应用：`services/api/src/zhijue/domain/questions.py`、`interview_policy.py`；`application/answer_workflow.py`、`interviews.py`、`operations_runner.py`。
- 适配/API：`adapters/model.py`、`adapters/db/models.py`、`adapters/db/operations.py`；`api/app.py`、`routes.py`、`schemas.py`、`errors.py`。
- 数据/契约：`migrations/versions/2c8f1d7a90b4_m3_01_answer_operation_link.py`、`contracts/openapi.json`、`api.md`。
- 测试：`tests/test_interview_runtime.py`、`tests/unit/test_questions.py`、`test_interview_policy.py`、`test_answer_workflow.py`、`test_operations_repository.py`。
- 配置/文档：`config/environment.env.example`、根与服务 README、`docs/02-architecture.md`、`docs/03-data-model.md`、`docs/04-workflow-policy.md`、`docs/07-test-and-acceptance.md`、`docs/13-risks-and-decisions.md`、`CHANGELOG.md`、`process.md`、`CHECKSUMS.sha256`、本交接。
- 前端契约同步：`apps/web/src/api.ts` 增加 Question/Answer/Decision/Operation 完整网络类型及 start/answer/retry 客户端；`App.tsx` 只纠正已批准 Seed 和后端就绪状态文案，没有新增答题交互。

接口/字段/状态/迁移变化：新增 `POST /interviews/{id}/start`、`POST /interviews/{id}/answers`、`POST /operations/{id}/retry`；Interview 视图返回当前题、已受理 Answer 与 root result；Operation result 返回 policy action、answer/observation/decision/next question 与 usage。迁移新增 Answer→accepted Operation 唯一外键，Question.seed_id 改为 nullable，Decision.target 允许 END 时为空。  
有意未实现的内容：前端答题组件、交互状态机与视觉设计；评分与报告；题库扩容；Memory/OCR/Web Fetch。前端仅同步网络契约和事实文案；依赖锁无变化、没有新增第三方依赖。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `.venv/bin/ruff format --check src tests migrations && .venv/bin/ruff check src tests migrations && .venv/bin/python -m pytest tests -q` | Linux x86_64 / Python 3.11.16 / openjiuwen 0.1.18 @ `72c4985111b8` | 0 | 58 文件格式/静态检查通过；247 passed / 2 skipped / 0 failed / 54 warnings | `services/api/tests/`、`runtime/evidence/m3-01-02/backend-verification.json` |
| `.venv/bin/python ../../tools/validate_spec.py` | 项目 venv | 0 | 44/44 passed | `validation-report.md` |
| `.venv/bin/python ../../scripts/doctor.py --json` | 项目 venv；私密值不回显 | 0 | 终态结果见 `backend-verification.json` | `runtime/evidence/m3-01-02/backend-verification.json` |
| FastAPI fixture smoke：plan→start→answer→operation/interview 查询 | 正式 API/SQLite/真实 openJiuwen Workflow；只替换外部 Analyzer | 0 | HTTP 202；operation succeeded；action NEXT；下一 main question；私人标记未进入 stdout/stderr | `runtime/evidence/m3-01-02/backend-verification.json` |
| `pnpm run build` | 锁定 Node 24 / pnpm 10.34.5 | 0 | TypeScript `--noEmit` 与 Vite build 通过，29 modules | `apps/web/src/api.ts`、`App.tsx` |
| 浏览器 fixture 视觉检查 | Chromium + Vite；API 响应显式 synthetic fixture | 0 | approved Seed、后端契约就绪、“前端待设计”同时显示；无旧 technical_review 文案 | `runtime/evidence/m3-01-02/backend-verification.json` |
| 业务文本模型 live | 需要显式 0600 私密配置中的六个业务模型变量 | — | `NOT_RUN`：当前 `.env.local` 未配置这些键 | 本交接“待负责人事项” |

实际模型/提示词/题库版本：外部模型 `null`；`OBSERVATION_SYSTEM_PROMPT` 为 `adapters/model.py` 当前实现；Seed 0.2.1，批准指纹 `1c6716b90449d375`；Policy 1.0.0。  
实际调用数/token/费用：外部文本模型调用 0；input/output/total token 为 `null`；cost 为 `null`。fixture smoke 调用 ScriptedAnalyzer 一次，不计外部 token/费用。  
是否有隐式 mock/回放：无回放；ScriptedAnalyzer 是显式外部模型测试替身。openJiuwen Workflow、语义校验、Policy、FastAPI、SQLite、并发单写入、事件、retry 和恢复均运行正式实现。  
仍然失败的样本与最小复现：最终本地回归无失败。业务模型 live 缺配置，不是测试失败；保持 `NOT_RUN`。首轮失败及修复已保留在 `process.md` §29。

## 同步与继续

api.md：已先于实现更新 start/answer/retry、Operation/Interview 视图、幂等/retry 与前端消费契约。  
process.md：已更新 M3 状态、实际命令、失败留痕、模型边界和唯一下一任务。  
架构/数据/测试/配置/CHANGELOG：根与服务 README、前端网络类型、`docs/02`、`docs/03`、`docs/04`、`docs/07`、`docs/13`、环境模板、OpenAPI、测试、CHANGELOG 均已同步；没有把契约同步伪装成前端答题界面已设计。  
新风险、待负责人事项：当前 `.env.local` 只有 embedding/Knowledge 键，缺 `MODEL_PROVIDER`、`MODEL_NAME`、`API_BASE`、`API_KEY`、`MODEL_TIMEOUT`、`MODEL_MAX_RETRIES`。负责人提供显式业务模型私密配置和预算前，M3-01 不得标 live VERIFIED。额外官方 starter/base-agent 要求仍 UNCONFIRMED/BLOCKED，不冒充已排除。  
唯一下一任务：前端答题界面设计评审；本轮停在设计前通知，不自动设计或施工。评审须覆盖问题/追问理由、UNKNOWN/synthetic 边界、operation pending/failed/interrupted/retry、revision 冲突、刷新恢复和隐私。  
继续时先运行的命令：`cd services/api && uv run python -m pytest tests/test_interview_runtime.py -q`；若负责人补齐模型配置，再显式设置 `ZHIJUE_MODEL_ENV_FILE` 运行 live 验收，不从 ambient env 猜配置。  
回滚方式：按本交接文件清单恢复修改文件并删除本轮新增模块/迁移/测试；不使用 `reset --hard`、stash 或清理用户工作区。