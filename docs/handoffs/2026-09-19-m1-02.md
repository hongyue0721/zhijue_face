# AI 施工交接：2026-09-18 / M1-02

## 当前状态

工作目录/分支：仓库根 / `master`（仓库尚无 commit）
基线 commit：`NO_COMMITS`；任务前状态以 M1-01 交接与本文件"回滚方式"中的新增路径清单为准  
本次新增：`services/api/src/zhijue/domain/{errors,extraction}.py`、`adapters/pdf.py`、`adapters/db/documents.py`、`application/{__init__,documents}.py`、`tests/fixtures_pdf.py`、`tests/unit/test_document_import.py`；修改：process、CHANGELOG、README、docs/07、CHECKSUMS、本文件  
应用版本 / 规范版本：规范 1.0.0；Python 3.11.16；pypdf 6.19.0（uv.lock 内既有）  
当前阶段和任务状态：M1 IN_PROGRESS；M1-02 VERIFIED（本地）；M0-BASE/SPEC-ALIGN BLOCKED；未自行 ACCEPTED  
运行模式：无网络、无模型（0 次调用；私有 PDF 为本地读取）

## 本次真实完成

解决的问题及关联 R 编号：docs/12 M1-02（PDF/文本导入和块定位）；R02 的导入与定位子项（确认交互属 R03/M1-03）；T01—T04 的持久层语义。  
修改文件：见上。  
接口/字段/状态/迁移变化：HTTP API/DTO/SSE/前端无变化；`api.md` 未修改；数据库迁移无新增（Document/SourceBlock 表 M1-01 已建）。  
有意未修改的内容：Claim 确认/快照/Knowledge 激活（M1-03）、`/facts` ProfileService（M1-03）、HTTP multipart 流式落盘（接口层施工决定）、OCR（P1）。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `pytest tests/unit/test_document_import.py` | 项目 venv + 临时 SQLite | 0 | 14 passed（首跑 collection exit 2 为红） | 过程记录于 process §21 |
| `.venv/bin/python -m pytest tests -q`（全量） | 项目 venv | 0 | **107 passed / 1 skipped / 0 failed / 38 warnings** | `runtime/evidence/m1-01/pytest-final.log` 为上轮；本轮收尾复跑记录于 process §21 |
| Ruff check + format --check（src/tests，25 文件） | ruff 0.16.8 | 0 | 全通过（4 处自动修复为 lint 卫生） | 同上 |
| Demo Resume v1 私有原件导入 | 本地 0600 文件 | 0 | 与 ignored 接收登记一致；原件、身份信息、指纹和正文未进入公开仓库 | 本地 ignored 去敏证据 |
| LLM / Knowledge / HTTP E2E / 浏览器 | — | — | NOT_RUN | — |

实际模型/提示词/题库版本：无。实际调用数/token/费用：0 / null / null。  
是否有隐式 mock/回放：无。合成 PDF 为显式 synthetic fixture，不冒充真实简历；真实样本走私有本地路径。  
仍然失败的样本与最小复现：无失败。失败保留：① 首版 fixture 把 UTF-8 字节直写 PDF literal，pypdf 提取出 mojibake → 测试暴露 → 改 ToUnicode CMap hex bfchar，实测中文正确；② 首版 `encrypt_pdf` 空 user 口令导致"加密"文档无口令可读 → 断言 `FileNotDecryptedError` 失败暴露 → 非空 user 口令修正。两处均为测试/fixture 缺陷，未删断言过关。

## 同步与继续

api.md：已检查无变化。  
process.md：§1/§2/§4/§21 已更新；M1-02 → VERIFIED，下一任务 M1-03。  
架构/数据/测试/配置/CHANGELOG：docs/07 追加 M1-02 记录；docs/03/05 无字段变化不改；CHANGELOG/README/CHECKSUMS 同步。  
新风险、待负责人事项：text/plain 内容当前在内存中校验（10 MiB 上限内）；M2-03 接口施工时决定是否流式落盘临时目录（docs/09 §7）。  
唯一下一任务：M1-03 声明确认、ProfileSnapshot 与 Knowledge 激活。  
继续时先运行的命令：`cd services/api && .venv/bin/python -m pytest tests -q`。  
回滚方式：删除本轮新增路径（domain errors/extraction、adapters/pdf.py、adapters/db/documents.py、application/、tests/fixtures_pdf.py、tests/unit/test_document_import.py、runtime/evidence/m1-02/）；被修改文件按 M1-01 交接记录的哈希恢复。不使用 reset/stash，不覆盖用户修改。
