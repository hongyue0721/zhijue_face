# AI 施工交接：2026-09-18 / M1-01

## 当前状态

工作目录/分支：仓库根 / `master`（仓库尚无 commit）
基线 commit：`NO_COMMITS`；任务前逐文件 hash 见 `runtime/evidence/m1-01/task-start.txt`  
本次 commit 或尚未提交的文件：未 commit/push；新增 `services/api/src/zhijue/`（2 domain + 3 db 模块）、`migrations/`（env + `18e3af0d1942`）、`alembic.ini`、`tests/unit/`（4 文件）、`runtime/evidence/m1-01/`；修改 `pyproject.toml`（pythonpath）、process/CHANGELOG/README/docs/07/CHECKSUMS 与本文件  
应用版本 / 规范版本：规范 1.0.0；Python 3.11.16；SQLAlchemy 2.0.54 / Alembic 1.20.0（uv.lock 内既有）  
当前阶段和任务状态：M1 IN_PROGRESS；M1-01 VERIFIED（本地）；M0-BASE/SPEC-ALIGN BLOCKED（不阻塞 M1）；未自行 ACCEPTED  
运行模式：无网络、无模型（fixture 级测试用临时 SQLite）；不是 integration_live

## 本次真实完成

解决的问题及关联 R 编号：docs/12 M1-01（表模型、迁移和 Operations）；为 R13 可恢复与 api.md §1 幂等提供持久层地基；docs/03 §3/§7 实体与约束落地。  
修改文件：见上。  
接口/字段/状态/迁移变化：新增初版 migration `18e3af0d1942`（14 表）。HTTP API/DTO/SSE/前端类型无变化；`api.md` 未修改。`pyproject.toml` 增加 `pythonpath=["src"]`。  
有意未修改的内容：ResumeDraft/TrainingMemory 不建表（M4-02/P1）；Knowledge 激活链（M1-03）；Operation 与业务资源的跨表校验（HTTP 接入时补）；旧 smoke/测试/证据。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `.venv/bin/python -m pytest tests -q`（全量） | Python 3.11.16 | 0 | 93 passed / 1 skipped / 38 warnings | `runtime/evidence/m1-01/pytest-final.log` |
| domain 纯函数测试（状态机/哈希/ID） | 同上 | 0 | 8 passed（首跑 collection exit 2 为红） | `tests/unit/test_domain_operations.py` |
| 仓储测试（幂等/事件/契约校验/恢复） | 临时 SQLite | 0 | 13 passed（首跑 exit 2 为红） | `tests/unit/test_operations_repository.py` |
| 迁移测试（可逆/零漂移/唯一键/FK） | alembic + 临时 SQLite | 0 | 5 passed；downgrade 仅剩 `alembic_version` | `tests/unit/test_migrations.py` |
| 并发红线（Barrier 4 线程 accept；双 claim running） | 临时 SQLite | 0 | 2 passed ×10 连跑稳定；首版实现被抓住抛 IntegrityError 缺陷并修复 | `tests/unit/test_operations_concurrency.py` |
| engine PRAGMA 测试（FK/busy_timeout/WAL 开关） | 同上 | 0 | 3 passed | `tests/unit/test_engine.py` |
| `ruff check` + `format --check`（src/tests/migrations） | ruff 0.16.8 | 0 | 全通过 | 收尾复跑 |
| LLM / Knowledge / E2E / 浏览器 | — | — | NOT_RUN（本任务不需要） | — |

实际模型/提示词/题库版本：无。实际调用数/token/费用：0 / null / null。  
是否有隐式 mock/回放：无；测试用独立临时库文件，未触任何端点。  
仍然失败的样本与最小复现：无失败。过程中修复的真缺陷（保留记录）：① `migrations/env.py` 曾无条件覆盖调用方 `sqlalchemy.url`，autogenerate/测试会把迁移打到真实 `runtime/business.db`——改为 注入 URL > 环境变量 > 默认 的优先级；② `accept` 并发输家收 IntegrityError 直接抛出，违反幂等契约——撞 `(scope,idempotency_key)` 后新事务重查返回赢家行，非竞争异常原样抛；③ 测试侧：单事务混合表插入 SQLAlchemy 按 add 顺序触发 FK——按 `Base.metadata.sorted_tables` 分表 flush。

## 同步与继续

api.md：已检查无变化。  
process.md：§1/§2/§4/§20 已更新；M1-01 → VERIFIED，下一任务 M1-02。  
架构/数据/测试/配置/CHANGELOG：docs/07 追加 M1-01 记录；docs/03 无字段变化（实现即按 §3/§7 落地），未改；CHANGELOG/README/CHECKSUMS 同步。  
新风险、待负责人事项：无新增外部依赖；Operation.resource_id 跨类型无 DB 级校验，HTTP 层接入时按 workspace 逐资源校验补应用层防线（已记 §20）。  
唯一下一任务：M1-02 PDF/文本导入与 SourceBlock 定位。  
继续时先运行的命令：`cd services/api && .venv/bin/python -m pytest tests -q`。  
回滚方式：删除本轮新增路径（src/zhijue、migrations、alembic.ini、tests/unit、evidence/m1-01），按 `runtime/evidence/m1-01/task-start.txt` 恢复被改文件；不使用 reset/stash。
