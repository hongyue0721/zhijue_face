# AI 施工交接：2026-09-19 M3-01 业务文本模型 live

## 当前状态

工作目录/分支：`zhijue-demo-spec-v1.0` / `main`  
基线 commit：`1077ba3d8429ed96091c39fd612a1f9245a3137e`  
本次 commit 或尚未提交的文件：本交接与下列代码/配置/文档由本轮单一提交落盘；最终 SHA 见 `process.md` 后续发布记录和交付回复  
应用版本 / 规范版本：Demo / 1.0.0  
当前阶段和任务状态：M3-01 `VERIFIED`；M3-02 `VERIFIED`；M3-03 `VERIFIED / FROZEN`；M4-01 `PLANNED`  
运行模式：`live`，输入为明确 synthetic 问答；不是回放

## 本次真实完成

解决的问题及关联 R 编号：闭合 R10/R11/R14 的真实业务回答模型边界。approved Seed、生产 Answer Analyzer、真实 openJiuwen Workflow、Observation 服务端语义校验和确定性 Policy 已在一个 synthetic UART/DMA 回答上完成 live 调用；没有把模型输出直接当动作或分数。

修改文件：

- 模型/Workflow：`services/api/src/zhijue/adapters/model.py`、`services/api/src/zhijue/application/answer_workflow.py`。
- live 验证：`services/api/smoke/answer_model.py`、`services/api/tests/unit/test_answer_workflow.py`。
- 配置：`config/environment.env.example`；真实 Key 仅在本机 0600、Git ignored 的 `.env.model.local`。
- 状态与交接：根与服务 README、`CHANGELOG.md`、`process.md`、`docs/07-test-and-acceptance.md`、`docs/13-risks-and-decisions.md`、`validation-report.md`、`CHECKSUMS.sha256`、本文件。

接口/字段/状态/迁移变化：HTTP API、OpenAPI、DTO、数据库 Schema、迁移、SSE、前端网络契约和四动作 Policy 均无变化。Prompt 现在明确 Observation 枚举、criterion 复制、level、原文引文和 reviewed reference 规则；SemanticValidation 传给 SDK 的失败异常改为固定消息，避免不可信模型字段进入 ERROR 日志。

有意未修改的内容：前三页 UI 继续冻结；未实现评分、报告、improved answer、Resume Draft、真实模型浏览器整场、429/超时 live 或批量效果评测；未新增依赖，未重复调用 Knowledge/embedding。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `PYTHONPATH=src .venv/bin/python -m smoke.answer_model --env-file ../../.env.model.local --output ../../runtime/evidence/m3-01-live/deepseek-flash-20260919T1855.json` | Python 3.11.16 / openjiuwen 0.1.18 @ `72c4985111b8` / `deepseek-flash` | 1 | 1 次 HTTP 请求真实返回；`finding` 为解释文本，Schema 拒绝；8.729782 秒 | ignored 失败证据 JSON |
| 同命令，output 改为 `deepseek-flash-20260919T1858.json` | 同上；Prompt 与日志边界修复后 | 0 | 1 次 HTTP 请求；真实 Workflow passed；Policy `NEXT / ADEQUATE_EVIDENCE`；10.650749 秒 | ignored 成功证据 JSON，SHA-256 `239a9a03a51b40a4f9ce8ee4d7f48a20d69240f90ddd30499bca039e6011b78b` |
| `.venv/bin/python -m pytest tests/unit/test_answer_workflow.py -q` | 项目 venv | 0 | 21 passed / 10 warnings；包含无效模型字段不进入 SDK 错误/日志的回归 | `services/api/tests/unit/test_answer_workflow.py` |
| `.venv/bin/ruff format --check src tests smoke migrations && .venv/bin/ruff check src tests smoke migrations && .venv/bin/python -m pytest tests -q` | 项目 venv | 0 | 63 files；248 passed / 2 skipped / 55 warnings | 本轮命令输出 |
| live `create_default_app()` 装配检查 | 独立 ignored SQLite；Knowledge/model 使用两个显式私密文件 | 0 | ready=true；Knowledge/model configured；SeedBank `1c6716b90449d375` | 本轮命令输出 |
| `services/api/.venv/bin/python tools/validate_spec.py` | 项目 venv | 0 | 44/44 passed | `validation-report.md` |
| `services/api/.venv/bin/python scripts/doctor.py --json` | 项目 venv | 0 | 18 PASS / 0 WARN / 0 FAIL；206 个 Git 可见文件密钥扫描 0 命中 | 本轮命令输出 |
| `sha256sum -c CHECKSUMS.sha256` | 项目根目录 | 0 | 206/206 OK | `CHECKSUMS.sha256` |
| `git diff --check` | 项目根目录 | 0 | 无空白错误 | 本轮命令输出 |

实际模型/提示词/题库版本：provider `deepseek`；model `deepseek-flash`；API origin `https://api.deepseek.com`；成功证据 Prompt SHA-256 `61883c52f7c206acfe940df5f3cb13a6bcbe1036cdd64a5b0c6e231984eccdf9`；Seed/rubric 0.2.1；approved review `review_m2_01_level2_owner_20260919`；Policy 1.0.0；openJiuwen 0.1.18 兼容 commit `72c4985111b8`。

实际调用数/token/费用：本轮 2 次逻辑模型调用、2 次 HTTP 尝试；第一次失败响应的 usage 未穿过 Workflow 失败边界，记 `NOT_MEASURED`；第二次 input 1156 / output 2544 / total 3700。两次 cost 均为 null / `NOT_MEASURED`，不按未知单价估算。

是否有隐式 mock/回放：没有。模型、HTTP transport、openJiuwen Workflow 均真实；输入为 synthetic；Knowledge 本轮 `NOT_RUN_THIS_SMOKE`，使用的是已批准 Seed 内冻结 reference points，不把本轮写成 Knowledge 再验收。

仍然失败的样本与最小复现：首轮失败证据保留在 ignored runtime；根因和修复见上表。真实 429/超时、费用预算耗尽、批量稳定性、p95、真实用户材料和浏览器真实模型整场仍 `NOT_RUN`。

## 同步与继续

api.md：已检查无变化；本轮没有 HTTP 契约、错误码或状态枚举变化。  
process.md：已更新 M3-01 为 `VERIFIED`、O04 为部分完成，并把唯一下一任务切到 M4-01。  
架构/数据/测试/配置/CHANGELOG：架构和数据模型无变化；测试、环境模板、风险、验收、README、CHANGELOG、完整性清单已同步；无新依赖。  
新风险、待负责人事项：单样本不能外推效果或 p95；持续付费调用上限和价格仍未确认。API Key 不得进入浏览器、Git、日志或公开证据。  
唯一下一任务：M4-01 评分与报告；必须先同步 `api.md`，再实现后端评分/UNKNOWN 边界、报告持久化和可恢复生成，不能先做假 Report UI。  
继续时先运行的命令：`cd services/api && .venv/bin/python -m pytest tests/unit/test_answer_workflow.py tests/test_interview_runtime.py -q`。  
回滚方式：恢复本交接列出的修改文件并删除 `smoke/answer_model.py` 与本交接；不使用 reset/stash，不删除 ignored 历史证据或覆盖用户修改。
