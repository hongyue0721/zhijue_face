# AI 施工交接：2026-09-19 / M3-03

## 当前状态

工作目录/分支：公开仓库工作区 / `main`  
基线 commit：`f2f3af2`  
本次 commit 或尚未提交的文件：前端实现 `5fe92051c09ef94e0009fc456546e69358905efb`；本交接、规范同步与完整性清单由包含本文件的后续 docs commit 落盘  
应用版本 / 规范版本：Demo / 1.0.0  
当前阶段和任务状态：M3-03 `VERIFIED`；M3-01 业务文本模型 live `NOT_RUN`；未写 `ACCEPTED`  
运行模式：浏览器 E2E 为显式 `fixture`；真实 FastAPI/SQLite/Operation/SSE/openJiuwen Workflow，只有外部 Answer Analyzer 使用 `ScriptedAnalyzer`

施工时间：2026-09-19T08:13:10-07:00—2026-09-19T09:03:51-07:00。

## 本次真实完成

解决的问题及关联 R 编号：把旧的资料/计划单页工作台改造成可验证的 P0 三页纵切面，覆盖 R-01—R-06 的当前 M1—M3 范围；没有提前实现 R-07 评分报告或 R-08 回答优化。  
修改文件：

- 前端入口与基础层：`apps/web/src/{App,api,main,presentation,routing,storage,styles}.ts*`。
- 页面：`apps/web/src/pages/{StartPage,PreparePage,InterviewPage}.tsx`。
- 组件与状态：`apps/web/src/components/{common,layout,profile,prepare,interview}/`、`apps/web/src/hooks/useOperationMonitor.ts`。
- 测试与依赖：`apps/web/tests/contracts.test.ts`、`apps/web/package.json`、`apps/web/pnpm-lock.yaml`、`.github/workflows/ci.yml`。
- 契约/架构/UX/验收记录：`docs/ui-contract.md`、`docs/{02-architecture,07-test-and-acceptance,08-ux}.md`、`README.md`、`CHANGELOG.md`、`process.md`、本交接。

接口/字段/状态/迁移变化：HTTP API、OpenAPI、Python DTO、数据库 Schema 和迁移均无变化。前端按既有 snake_case 契约补齐 Document/blocks、facts/confirm、JD options、Interview start/answer 与 Operation retry 消费；202 只表示 accepted，业务结果只取 Operation 终态和资源快照。  
有意未修改的内容：没有新增 control/report/resume/list/runtime endpoint；没有评分、报告、回答优化、历史列表、登录、职位市场、OCR 或 Memory；没有把 internal ID 当业务文案；没有 Liquid Glass。

依赖说明：

- `@any-design/anyui@0.5.2`：负责人指定的组件基础，直接从包入口导入，不复制组件源码。
- `@iconify/react@6.0.2`：AnyUI React 包运行时直接导入的 peer，缺少时无法构建；不是第二套 UI 框架。
- `vitest@4.0.18`：为纯前端契约边界提供可重复回归并接入 CI。三者均可锁定版本本地安装，不增加云服务或运行时基础设施。

## 验证证据

| 命令/用例 | 环境 | 退出码 | 通过/失败/未运行 | 证据路径 |
|---|---|---:|---|---|
| `corepack pnpm@10.34.5 test` | Node 24 / pnpm 10.34.5 | 0 | 1 file，7 passed | `apps/web/tests/contracts.test.ts` |
| `corepack pnpm@10.34.5 build` | Node 24 / pnpm 10.34.5 | 0 | TypeScript `--noEmit` + Vite，112 modules | 本轮命令输出 |
| `.venv/bin/python -m pytest -q` | Python 3.11 | 0 | 247 passed / 2 skipped / 54 warnings | 后端测试集 |
| FastAPI + Vite 浏览器纵切面 | Chromium；390/768/1366/1440/1920，六组视口 | 0 | PDF→blocks→fact→confirm→JD→plan→start→answer→PROBE/CLARIFY/NEXT/END；失败重试和 SSE 降级通过 | `runtime/evidence/m3-03/browser-e2e.json`、`ui-*.png`（ignored） |
| `tools/validate_spec.py` 首轮 | Python 3.11 | 1 | 43/44；README 已引用但 handoff 尚未落盘，缺链路按原错误保留 | 本交接创建前命令输出 |
| `tools/validate_spec.py` 终态 | Python 3.11 | 0 | 44/44 passed | `validation-report.md` |
| `scripts/doctor.py --json` 终态 | Python 3.11 | 0 | 18 PASS / 0 WARN / 0 FAIL | 本轮命令输出 |
| `sha256sum -c CHECKSUMS.sha256` | coreutils | 0 | 202/202 一致 | `CHECKSUMS.sha256` |

浏览器还实际验证：multipart 没有被强制写成 JSON Content-Type；演示 JD 请求不自报 `source_type`；answer 网络不确定性重试复用完全相同的 body、`client_turn_id` 与 `Idempotency-Key`；分析失败重试只走 `/operations/{id}/retry`；阻断 SSE 后 4 次 Operation polling 仍推进到下一主问题；390×844、768×900、1366×768、1440×900、1440×1000、1920×1080 六组视口均无横向溢出。

实际模型/提示词/题库版本：openJiuwen 0.1.18（项目兼容 commit `72c4985111b8`）；Answer Analyzer 为显式 fixture；SeedBank 0.2.1、六条 approved。  
实际调用数/token/费用：外部业务文本模型调用 0；provider/model = null；input/output token = null；cost = null。  
是否有隐式 mock/回放：无隐式 mock；浏览器服务横幅明确显示 `fixture`，只替换外部 Analyzer，未替换 FastAPI、SQLite、Operation/SSE 或 openJiuwen Workflow。  
仍然失败的样本与最小复现：产品终态无已知失败。施工中首次规范校验因 handoff 尚未创建而 43/44；浏览器自动化曾遇到 helper 等待超时和一次 locator 误用，页面状态实际已完成，改用 DOM 状态断言后继续；视觉检查发现 AnyUI 全局 `span` 与 `textarea` 暗色样式污染，已用应用作用域样式修复并重新截图。真实业务文本模型 live 未运行，不属于通过项。

## 同步与继续

api.md：已检查无变更；现有 route、字段、错误码、202/Operation 与 SSE 语义足够，前端没有反向创造接口。  
process.md：已更新。  
架构/数据/测试/配置/CHANGELOG：架构树、三页 UX、UI/API 对照、验收矩阵、README、CHANGELOG 和 CI 已同步；数据模型与运行配置无变化。  
新风险、待负责人事项：O04 仍缺六个业务文本模型私密配置与费用上限；跨新标签页恢复失败 operation ID、跨代理 UTF-8 分块、`EVENT_HISTORY_GONE` 浏览器组合未验证；M4 报告不存在。  
唯一下一任务：负责人提供 O04 后，执行 M3-01 业务文本模型 live 验证；在此之前不启动 M4，也不把 fixture 指标写成模型效果。  
继续时先运行的命令：`services/api/.venv/bin/python scripts/doctor.py --json`，再按私密配置运行 M3-01 live 专项；禁止在聊天、Git 或浏览器配置中暴露密钥。  
回滚方式：以 `f2f3af2` 为文件级基线；删除本轮新增的页面/组件/hooks/测试/UI 契约和 handoff，并按本文件清单恢复已有文件。不得使用 `reset --hard`、stash 或覆盖后续用户修改。
